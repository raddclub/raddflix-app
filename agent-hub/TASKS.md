 ✅ DONE | `10aaa85` | ✅ DONE | `10aaa85` | ✅ DONE | `10aaa85` | ✅ DONE | `10aaa85` | ✅ DONE | `10aaa85` | ✅ DONE | `10aaa85` | ✅ DONE | `10aaa85` | ✅ DONE | `10aaa85` |# RaddFlix Tasks

> This is the single live task board. Start every session at `AGENT_PROMPT.md`.
> Add a row here (⏳ IN PROGRESS) before starting work, mark ✅ DONE + commit SHA when pushed.
>
> **This file is a lean INDEX, not a full history.** Every completed item below has its full
> write-up (root cause, implementation notes, testing checklist) already preserved in
> `agent-hub/history/TASK_LOG.md` — search that file by phase name or ID before re-deriving
> something that may already be documented. Keeping this file short means every future session
> reads less and starts working faster; nothing is lost, it's just filed correctly.
>
> See `agent-hub/ANIMATION_PLAN.md` for animation spec and acceptance criteria.

---

## Open Tasks

| Task | Description | Plan | Status |
|---|---|---|---|
| AUDIO-DISC-BUGS | **Audio player: tonearm position, double groove paint, spindown reset-in-listener.** (1) `_Tonearm` used `Positioned(top:8,right:8)` — anchored to screen edge, pivot ~60dp off the disc on a 360dp phone. Fixed: `Align(center)` + `Transform.translate(Offset(60,-58))` puts pivot on disc rim. (2) `CustomPaint` had both `painter:` and `foregroundPainter:` pointing to separate `_GroovePainter` instances — grooves and sheen arc drawn twice (double opacity). Fixed: removed `painter:`, kept only `foregroundPainter:`. (3) `_spinDownCtrl.reset()` called synchronously inside its own `addStatusListener` — fires a second `notifyListeners()` during dispatch. Fixed: deferred to `addPostFrameCallback`. | `audio_mode_backdrop.dart` | ✅ DONE — `f59bebf` CI ✅ |
| HOME-FILTER-CHIP | **Home screen: replace filled-pill category chips with quiet text underline filters.** `_CategoryChip` still used old gradient fill, border, box shadow, and check icon despite the HOME-REDESIGN-2026 brief specifying plain text + red underline. No other layout changes needed — everything else from the brief was done in HOME-REDESIGN-2026. | ✅ DONE | 2026-07-28 | `b320f40c` CI ✅ |
| HOME-FILTER-CHIP-CLEANUP | **Home screen shimmer + import cleanup.** Unused `RaddMotion` import left behind after chip refactor. Shimmer skeleton showed two CTA button placeholders (real hero has one) and rounded pill chip placeholders (real chips are now text-style). All three fixed. | ✅ DONE | 2026-07-28 | `2f2918b9` CI ✅ |
| HOME-REDESIGN-2026 | **Home screen visual hierarchy reset.** Implement the uploaded Home redesign brief without changing the underlying catalog, playback, navigation, or subscription behavior. Sequence: (1) make the hero edge-to-edge, remove its outer card treatment, shorten it to ~220–235dp, blend its bottom into the exact Home background, move dots inside the fade, and keep only Resume/Watch Now; (2) place Continue Watching immediately after the hero when available; (3) move SIMOSA below the first primary shelf and replace its first-viewport card with a compact relevant reminder that can still open the full feature; (4) replace category pills with quieter text filters and a selected underline; (5) reduce default shelves to Continue Watching, one priority shelf (Trending Now or New Arrivals), Movies, and TV Shows & Dramas, while preserving access to other content through filtering/conditional rendering; (6) simplify shelf headers and reduce repeated poster decoration/badges/glints/borders/shadows while retaining title, essential status, and progress affordances; (7) tighten greeting/app-bar spacing, remove the waving-hand emoji, and slow or remove automatic hero rotation. Preserve full accessibility, responsive behavior, existing tap/long-press/detail/resume flows, and the floating player/bottom navigation. | `raddflix_flutter/lib/screens/home_screen.dart`, `raddflix_flutter/lib/widgets/content_card.dart`, `raddflix_flutter/lib/widgets/simosa_card.dart`, `raddflix_flutter/lib/widgets/mini_player_bar.dart` only if needed | ✅ DONE — `42386f9` code + `30355793350` CI ✅ |
| AUTH-UX-2026 | **Auth experience refresh.** Give login and registration a stronger RaddFlix identity with progressive registration, live validation, responsive feedback, and subtle micro-interactions while preserving the existing phone/password, guest, device-conflict, and API flows. | `login_screen.dart`, `register_screen.dart` | ✅ DONE — `39343b04` code + `30263339764` CI |
| THEME-V2-01 | **WCAG textMuted contrast fix — all themes (P0 accessibility).** Every dark variant theme has `textMuted` failing WCAG AA on its background. Measured ratios: dark 2.75:1 ❌, midnight 3.51:1 ⚠️, cobalt 3.36:1 ⚠️, rose 3.33:1 ⚠️, navy 4.33:1 ⚠️, forest 3.91:1 ⚠️, charcoal 3.92:1 ⚠️, light 4.27:1 ⚠️. Fix: push each to ≥4.5:1 using pre-validated replacement values. AMOLED already passes at 4.57:1, unchanged. `AppColors.textMuted` + `lightTextMuted` in constants.dart, all `textMuted` tokens in `radd_theme.dart`. | `constants.dart`, `radd_theme.dart` | ✅ DONE — `13de8bc` |
| THEME-V2-02 | **backgroundAlt invisible-step fix.** `AppColors.backgroundAlt` `#121214` is only 1.04:1 from bg `#0D0D0F` — visually indistinguishable (Netflix's equivalent step is 1.16:1). Push to `#1D1D20` for a 1.15:1 step. Update `darkGradient` and `AppGradients.dark` second stops to match. Update `dark.bgAlt` in `radd_theme.dart`. | `constants.dart`, `radd_theme.dart` | ✅ DONE — `13de8bc` |
| THEME-V2-03 | **Pakistani identity hero-gradient signature.** Add `AppColors.heroIdentity = Color(0xFF0A0A1E)` (midnight-indigo) and `AppGradients.heroIdentity` (transparent → `#0A0A1E`) as a Pakistani night-sky hero-overlay option. Gives the app a distinctive depth cue vs generic neutral-black — separates RaddFlix visually from Netflix-clone territory. | `constants.dart` | ✅ DONE — `13de8bc` |
| THEME-V2-04 | **Document brand-primary AA-large-only rule.** Primary `#C41E3A` is 3.32:1 on dark bg — AA-large-only. Add Rule 11 to `AI_RULES.md` prohibiting its use as a text colour on body copy (<14sp bold / <18sp regular). | `docs/design-system/AI_RULES.md` | ✅ DONE — `13de8bc` |

| Task | Description | Plan | Status |
|---|---|---|---|
| UX4-01 | **Tab navigation → IndexedStack (P0).** All 4 non-Home tabs use `Navigator.pushNamed` — each tap pushes a new route, losing scroll position, search text, and all state. Refactor `HomeScreen` into a shell widget with an `IndexedStack` of 5 persistent children (Home feed, Search, Local, Downloads, Profile). Each child gets `AutomaticKeepAliveClientMixin` so state is preserved across switches. `RaddFlixBottomNav` + `MiniPlayerDock` live at the shell level. Remove `Navigator.of(context).pushNamed(AppRoutes.search/localMedia/downloads/profile)` calls from `onTap`; replace with `setState(() => _navIndex = i)`. Deep-link routes for those screens remain in `app.dart` for other callers. | `home_screen.dart`, `profile_screen.dart`, `app.dart` | ✅ DONE — `a450fe0a` |
| UX4-02 | **Remove unplayable TMDB "You Might Like" section from home (P0).** `home_screen.dart` renders a full `_RecommendationsSection` carousel of TMDB posters that all show a SnackBar "not in library yet" on tap. Users repeatedly tap content they can't watch — confusing and trust-breaking. Remove the section entirely from `_buildContent`. The `catalog.recommendations` data and the TMDB fetch can stay for future use; only the rendered carousel is removed. | `home_screen.dart` | ✅ DONE — `d0526d53` |
| UX4-03 | **Remove global `textScaler: TextScaler.linear(1.0)` forced override (P0 — accessibility).** `app.dart:203` wraps the entire app in `MediaQuery.copyWith(textScaler: TextScaler.linear(1.0))`, disabling Android's system font-size accessibility for all users. `bottom_nav.dart:241` also uses `textScaler: TextScaler.noScaling` on nav labels. Remove both — restore system font scaling. After removal, audit for overflow: test nav labels, badge texts, and card titles at 1.5× font scale and fix any overflow with `maxLines`/`overflow: TextOverflow.ellipsis` guards. | `app.dart`, `bottom_nav.dart` | ✅ DONE — `58ba3d38` |
| UX4-04 | **ShowDetail scroll → `ValueNotifier` to stop full-tree rebuilds (P1 — performance).** Replace `_scrollOffset` state var with a `ValueNotifier<double>`. Wrap only the parallax hero image in a `ValueListenableBuilder`. | `show_detail_screen.dart` | ✅ DONE |
| UX4-05 | **Mirror theme picker to Settings screen (P1).** Theme picker (`_ThemePicker` + `_ThemeTrailing`) currently lives as private classes in `profile_screen.dart`. Move both to a new shared file `lib/widgets/theme_picker_sheet.dart` (make them public). Update `profile_screen.dart` import. Add an "Appearance" `_SettingsSection` to `settings_screen.dart` with a `SettingsRow` that shows the current theme swatch + name as trailing and opens the picker bottom sheet on tap — matching the Profile UX exactly. | `lib/widgets/theme_picker_sheet.dart` (new), `profile_screen.dart`, `settings_screen.dart` | ✅ DONE |
| UX4-06 | **AppBar scroll opacity → smooth interpolation (P1 — polish).** `home_screen.dart:61–63` uses a binary bool `_scrolled` that snaps AppBar opacity at exactly 50px scroll (transparent → 96% opaque in one frame). Replace with a `ValueNotifier<double>` tracking raw offset; interpolate opacity as `(offset / 200).clamp(0.0, 1.0)`. Remove `setState` from the scroll listener entirely — use `ValueListenableBuilder` scoped only to the AppBar. | `home_screen.dart` | ✅ DONE |
| UX4-07 | **Hero carousel dots/page indicator (P1 — discoverability).** PageController + animated dots below hero. Active: primary 8px; inactive: textMuted@0.4 5px. Dots shown when count > 1. | `home_screen.dart` | ✅ DONE |
| UX4-08 | **Login form keyboard chain — `textInputAction` + `FocusNode` (P1 — form UX).** `login_screen.dart` Phone and Password `RaddTextField` widgets have no `textInputAction` or `FocusNode`. After typing the phone number, the user must physically tap the password field. Add `textInputAction: TextInputAction.next` + `focusNode: _phoneFocus` / `nextFocusNode: _passFocus` to the phone field; `textInputAction: TextInputAction.done` + `onSubmitted: (_) => _login()` to the password field. Same fix in `register_screen.dart` for its equivalent fields. | `login_screen.dart`, `register_screen.dart` | ✅ DONE |
| UX4-09 | **Fix undersized tap targets — 22px remove button, 16px mini player controls (P2 — accessibility).** (1) Continue-Watching remove X: `home_screen.dart:1098–1107` — `Container(width: 22, height: 22)`. Enlarge to at least 44×44 via `SizedBox(width: 44, height: 44)` wrapping the icon, keeping visual size the same with padding. (2) Audit `mini_player_bar.dart` dismiss X and play/pause buttons — raise all touch targets to ≥44dp using `SizedBox` + `IconButton` pattern. All visual sizes stay the same; only the hit area grows. | `home_screen.dart`, `mini_player_bar.dart` | ✅ DONE |
| UX4-10 | **Pakistani phone number formatter on login/register (P2 — polish).** Add a TextInputFormatter that auto-inserts a hyphen after position 4 (03XX-XXXXXXX format, max 12 chars including hyphen) to the phone field in login_screen.dart and register_screen.dart. Validate stripped digits are exactly 11 chars starting with 03. Strip hyphen before API call in _login()/_register(). | login_screen.dart, register_screen.dart | ✅ DONE — `fb5d989b` |

> This is the single live task board. Start every session at `AGENT_PROMPT.md`.
> Add a row here (⏳ IN PROGRESS) before starting work, mark ✅ DONE + commit SHA when pushed.
>
> **This file is a lean INDEX, not a full history.** Every completed item below has its full
> write-up (root cause, implementation notes, testing checklist) already preserved in
> `agent-hub/history/TASK_LOG.md` — search that file by phase name or ID before re-deriving
> something that may already be documented. Keeping this file short means every future session
> reads less and starts working faster; nothing is lost, it's just filed correctly.
>
> See `agent-hub/ANIMATION_PLAN.md` for animation spec and acceptance criteria.

---

. | `login_screen.dart`, `register_screen.dart` | ✅ DONE |
| UX4-11 | **GlintOverlay performance — raise tier gate (P2 — performance/battery).** Every `ContentCard` runs its own permanent `AnimationController` that fires a 680ms animation every ~4.3 seconds. With 30–50 cards on screen this is 30–50 concurrent timers rebuilding at 60fps — significant on low-end Pakistani devices. Quick fix: change the gate from `animConfig.canStagger` (Tier 1+, most devices) to `animConfig.canMorph` (Tier 3 only, high-end). One-line change in `content_card.dart:110`. This preserves the glint on flagship devices while eliminating the load on mid-range phones. | `content_card.dart` | ✅ DONE |
| UX4-12 | **Unify card long-press to bottom sheet (P2 — consistency).** `ContentCard` long-press uses `showMenu` (system popup anchored to press point). The continue-watching remove uses `showModalBottomSheet`. Two different patterns for the same gesture. Replace `showMenu` in `_showQuickActions` with a `showModalBottomSheet` bottom sheet containing the same 3 actions (Play / Watchlist toggle / More Info), styled with the existing `AppRadius.xl` rounded container pattern used throughout the app. | `content_card.dart` | ✅ DONE |
| UX4-13 | **Logout dialog → themed `AlertDialog` (P3 — consistency).** `profile_screen.dart:135` uses a raw `AlertDialog` with default Material styling (white background, default text) for Sign Out. `settings_screen.dart` dialogs already have `backgroundColor: t.card`, `shape: RoundedRectangleBorder(borderRadius: RaddRadius.mdRadius)`. Apply the same theming to the logout dialog to match. One-file, ~5 line change. | `profile_screen.dart` | ✅ DONE |
| UX4-14 | **Server-driven home categories (P3 — flexibility).** `home_screen.dart:49` has `static const _categories = ['All', 'Movies', 'Shows', 'Dramas', 'Urdu', 'Punjabi', 'English']` — hardcoded in Dart. Any new category requires a new APK release. Fetch category list from `/api/app/config` response (already called in `_checkForUpdates`). If the server returns a `home_categories` array, use it; fall back to the current hardcoded list if absent or on error. Categories update on next app launch without an APK update. | `home_screen.dart`, `core/api/api_client.dart` | ✅ DONE |
| PLANS-FORM-FIX-2026-07-16 | **Plans admin: "Add New Plan" and Edit not working.** Root cause: `onclick="editPlan({{ p|tojson }})"` put double-quoted JSON inside a double-quoted HTML attribute — browser closed the attribute at the first `"` in JSON, so clicking Edit executed the partial string `editPlan({` (SyntaxError) and silently did nothing. Add New Plan modal opened correctly but offered zero feedback (no toast/flash) so admin thought submissions failed. Fix: replaced tojson-in-onclick with an integer ID — `onclick="editPlan({{ p.id }})"` — and look up plan object from `_PLANS` map rendered once inside a safe `<script>` block. Added `?ok=` redirect params + JS toast for create/edit/toggle/delete confirmation. Added Escape key close for modal. | `radd-hub/hub/routes/plans_panel.py` | ✅ DONE — `61027c1b` |
| PLANS-NO-JS-FIX-2026-07-16 | **Plans admin: create/edit still not working after JS modal fix.** JS was correct and deploying fine but browser-side issue (extension, security policy, or other JS blocking) still prevented the modal from functioning. Diagnosis: entire create/edit flow depended on inline JS running to open the modal and set the form's `action` attribute — if JS is blocked at all, the form has no action and silently submits to the wrong URL. Fix: replaced the JS modal entirely with dedicated server-rendered pages: `GET /plans/new` (blank create form) and `GET /plans/<id>/edit_form` (pre-filled edit form, data fetched from DB server-side). "Add New Plan" card changed from `onclick="openModal()"` to `href="/plans/new"`. Edit buttons changed from `onclick="editPlan(id)"` to `href="/plans/X/edit_form"`. Success feedback uses `?ok=` banner rendered server-side (no JS toast needed). Zero JavaScript dependency for any CRUD operation. | `radd-hub/hub/routes/plans_panel.py` | ✅ DONE — `e1cb9da3` |
| PLANS-ADMIN-FIX-2026-07-16 | **Plans & Pricing admin panel shows 0 plans; app shows hardcoded fallback plans instead of DB plans.** Root causes: (1) `init_db()` never seeds default plans — `plans` table is empty so admin panel shows nothing; (2) `mobile_api.py` /api/subscription/plans reads features from `description` field instead of `features_json` — features always empty in app; (3) `tid_panel.py` uses hardcoded `PLAN_DURATIONS`/`PLAN_PRICES` dicts when approving TID payments — ignores admin-configured `duration_days`/`price_pkr`; (4) `upsert_plan()` DB helper missing `badge`, `color`, `features_json` cols. **Fixes: db.py** (seed 4 default plans matching fallback + fix upsert_plan cols); **mobile_api.py** (features_json fix); **tid_panel.py** (DB lookup on approve); **subscriptions.py** (remove dead PLAN_DURATIONS). | `radd-hub/hub/db.py`, `mobile_api.py`, `tid_panel.py`, `subscriptions.py` | ✅ DONE — `b545d78b` |
| VAULT-SPEED-2026-07-16 ✅ | **Vault bulk-add speed: eliminate serial awaits.** Current `_importVideoFolder`, `_processPickedFiles`, and `_addFolderToVault` use a `for-await` loop per file — each iteration resolves the vault folder, moves the file, and notifies MediaStore sequentially. For a 50-episode season this stacks to 50 × (folder lookup + rename IPC + MediaStore scan IPC). **3 changes, 3 files:** (1) `vault_service.dart` — new `moveFilesToVaultBatch(sourcePaths, {folder, onProgress})`: resolves target dir once, moves files in parallel chunks of 4 via `Future.wait` with per-file error isolation, fires all `notifyMediaStore` calls concurrently at the end. (2) `vault_screen.dart` — replace serial loops in `_importVideoFolder` and `_processPickedFiles` with `moveFilesToVaultBatch`; pre-collect content URIs before batch (sync); replace `for-await deleteVaultFile` in `_deleteSelected` with `Future.wait`. (3) `local_media_screen.dart` — replace serial loop in `_addFolderToVault` with `moveFilesToVaultBatch`, pre-collect content URIs sync before batch. No new packages. `deleteFromMediaStore` calls (already batched on the Kotlin side) unchanged. | `vault_service.dart`, `vault_screen.dart`, `local_media_screen.dart` | ✅ DONE — `9cf5631f` |
| APP-LOCK-2026-07-16 ✅ | **Full app-level PIN / biometric lock.** Locks the entire app on resume — not just the vault. User must enter PIN or use biometrics before any screen is accessible. Completely independent of the vault PIN (user can have both, one, or neither). **What to build — 5 components:** **(1) `AppLockService` (new file: `lib/services/app_lock_service.dart`, ~150 lines)** — mirrors `VaultService` pattern. Fields: `_isLocked` bool, `_pausedAt` DateTime?. Methods: `setPin(pin)`, `hasPin() → Future<bool>`, `checkPin(pin) → Future<bool>`, `removePin()`, `lock()`, `unlock()`, `get isLocked → bool`, `setBiometricEnabled(bool)`, `getBiometricEnabled() → Future<bool>`, `setAutoLockTimeout(int seconds)` (0 = immediately, -1 = never), `getAutoLockTimeout() → Future<int>`, `onAppPaused()` (record pause timestamp), `onAppResumed() → bool shouldLock` (compares elapsed vs timeout). PIN hashed with SHA-256 + salt `raddflix_app_lock_salt_` stored in `FlutterSecureStorage` under key `app_lock_pin_hash`. Auto-lock timeout stored in `SharedPreferences` under key `app_lock_timeout_seconds`. Biometric flag stored in `SharedPreferences` under key `app_lock_biometric`. Reuse already-installed packages: `flutter_secure_storage`, `local_auth`, `shared_preferences`, `crypto`. **(2) `AppLockScreen` (new file: `lib/screens/app_lock_screen.dart`, ~200 lines)** — full-screen lock UI shown over the entire app. Reuse `RaddLockPad` (already exists in `lib/design_system/components/radd_lock_pad.dart`) for the PIN pad — same component the vault uses. Show app logo / branding at top. Biometric button (fingerprint icon) shown only if `getBiometricEnabled() == true`; auto-triggers biometric prompt on screen open if biometric is enabled. On correct PIN: call `AppLockService.unlock()`, `Navigator.of(context, rootNavigator: true).pop()`. On wrong PIN: shake animation + attempt counter. No back button (use `PopScope(canPop: false)`). **(3) `MaterialApp.builder` gate + `WidgetsBindingObserver` in `lib/app.dart`** — wrap the existing `builder:` (or add one) to conditionally overlay `AppLockScreen` when `AppLockService.isLocked`. Make `_AppState` (or the root widget's State) implement `WidgetsBindingObserver`; in `initState` call `WidgetsBinding.instance.addObserver(this)`; override `didChangeAppLifecycleState`: on `paused` → `AppLockService.onAppPaused()`; on `resumed` → if `AppLockService.onAppResumed()` then show lock (push `AppLockScreen` via `appNavigatorKey` with `rootNavigator: true`). On cold start: in `initState`, if `AppLockService.hasPin()` and timeout is 0 (immediately), call `AppLockService.lock()` and push lock screen after first frame. **(4) `FLAG_SECURE` toggle in `MainActivity.kt`** — add `"setFlagSecure"` case to the existing `SECURITY_CHANNEL` handler: `"setFlagSecure" → { val on = call.argument<Boolean>("enabled") ?: true; if (on) window.addFlags(WindowManager.LayoutParams.FLAG_SECURE) else window.clearFlags(WindowManager.LayoutParams.FLAG_SECURE); result.success(null) }`. Add `import android.view.WindowManager` at the top. Dart side: add `AppLockService.setFlagSecure(bool)` that calls the channel. Call `setFlagSecure(true)` when app lock PIN is set, `setFlagSecure(false)` when removed. FLAG_SECURE hides app content from Android recents thumbnail and blocks screenshots while the lock screen is showing. **(5) Settings UI additions in `lib/screens/settings_screen.dart`** — add an "App Lock" section (similar to the existing Vault section in the profile screen). Items: Enable App Lock toggle (navigates to set-PIN flow if enabling for first time, or disables+removes PIN if turning off — show confirmation dialog before removal); Change PIN (only shown if PIN already set); Biometric unlock toggle (only shown if device supports biometrics — check via `LocalAuthentication().isDeviceSupported()`); Auto-lock timing `SettingsRow` with options: Immediately / After 30 s / After 1 min / After 5 min / Never. **Edge cases to handle:** (a) If app lock PIN and vault PIN are both set and identical — they are stored under different keys and are fully independent; no special handling needed. (b) If device has no biometric hardware — hide biometric toggle entirely. (c) FLAG_SECURE: apply on app-lock enable; remove on disable; also remove temporarily when the user is on the set-PIN / change-PIN screen so they can see what they're typing (call `setFlagSecure(false)` before showing that screen, restore on pop). (d) Auto-lock "Never" option: `onAppResumed()` returns false when timeout is -1, so the lock screen is never shown again after the first unlock — vault still has its own independent auto-lock. (e) Cold-start: if `isLocked` is true when `MaterialApp` builds, show lock screen immediately before any navigation completes (use `SchedulerBinding.instance.addPostFrameCallback`). (f) `context.mounted` / `mounted` guards on all async paths as per existing project conventions. **No new pub packages needed** — all required packages (`flutter_secure_storage`, `local_auth`, `shared_preferences`, `crypto`) are already in `pubspec.yaml`. **Files to change:** `MainActivity.kt` (+15 lines), `lib/services/app_lock_service.dart` (new, ~150 lines), `lib/screens/app_lock_screen.dart` (new, ~200 lines), `lib/app.dart` (~80 lines added), `lib/screens/settings_screen.dart` (~100 lines added). | `MainActivity.kt`, `lib/services/app_lock_service.dart` (new), `lib/screens/app_lock_screen.dart` (new), `lib/app.dart`, `lib/screens/settings_screen.dart` | ✅ DONE — `11950d5e` |
| VAULT-UX-2026-07-15 | Four vault bugs: (1) Restore hardcoded `/storage/emulated/0/Download` — fails on Android 11+ (WRITE_EXTERNAL_STORAGE maxSdkVersion=29), copy threw before delete so vault file never removed. (2) Files still visible in MX Player/file managers after vault add from Local screens — `deleteFromMediaStore` never called from `local_media_screen`/`local_folder_screen`. (3) No progress feedback on any multi-file vault operation — app felt frozen/heavy. (4) `local_folder_screen._addToVault` checked `hasPin` but not `isUnlocked` — could move file without entering PIN. | `MainActivity.kt`, `vault_service.dart`, `vault_screen.dart`, `local_media_screen.dart`, `local_folder_screen.dart` | ✅ DONE — `b91768e4` CI ✅ |
| BUG-DL-EXT-01 | Downloads saved every file with a hardcoded `.mp4` extension regardless of actual container — an MKV downloaded as `.mp4` broke vault cover-art detection and extension-gated open logic. Also: `LocalMediaService.getThumbnail` re-decoded a frame via MPV on every grid rebuild causing scroll jank in the Local Media screen; routed through `ThumbService` mem+disk cache. | `download_service.dart`, `local_media_service.dart` | ✅ DONE — `70334a63` CI ✅ |
| UX3-01 | Clear Image Cache is a destructive action with no confirmation — one accidental tap clears all cached posters. Add `showDialog` confirmation before executing `PaintingBinding.imageCache.clear()`. | `settings_screen.dart` | ✅ DONE — `613b6023` CI ✅ |
| UX3-02 | Watchlist sort order resets to "Date Added" every time the screen is opened — user preference is never saved. Persist `_sortBy` selection to `SharedPreferences` on change, restore on init. | `watchlist_screen.dart` | ✅ DONE — `722b360e` CI ✅ |
| UX3-03 | History screen server sync is fully fire-and-forget — if `mergeServerHistory()` throws, user sees stale data with zero feedback. Add try/catch and show a subtle dismissible error banner on failure. | `history_screen.dart` | ✅ DONE — `f8f3ecbe` CI ✅ |
| UX3-04 | MiniPlayerBar subscription-gate dismisses the bar and shows a snackbar when user taps resume on a paid title without an active subscription — abrupt and confusing. Show a proper "Subscribe to continue" bottom sheet instead of silently clearing the bar. | `mini_player_bar.dart` | ✅ DONE — `3cccc1df` CI ✅ |
| UX3-05 | Actor screen filmography grid uses plain `ContentCard` taps with no shared-element morph — the only content grid in the app still missing the `OpenContainer` pattern that Watchlist/History/Home/Search all have. Add tier-gated morph matching the pattern from Task 8. | `actor_screen.dart` | ✅ DONE — `dca97b15` CI ✅ |
| UX3-06 | Downloads vault dead-end: tapping "Move to Vault" when no vault PIN is set produces a confusing error. Detect the no-PIN state up front and show a "Set up Vault" prompt that routes the user to vault settings instead of blocking them with an error. | `downloads_screen.dart` | ✅ DONE — `cd4b0aa9` CI ✅ |
| UX3-07 | Profile Switcher zoom-morph is Tier 2+ only — Tier 0/1 users get a plain push that feels jarring compared to the polished "Who's Watching?" screen. Add a lightweight hero + scale animation for lower tiers that doesn't require `OpenContainer`. | `profile_switcher_screen.dart` | ✅ DONE — `2c13acf0` CI ✅ |
| UX3-08 | Show Detail screen is 3,007 lines with episode list, cast rail, and related rail all inline — untestable and unmaintainable. Extract `_EpisodeListSection`, `_CastRailSection`, and `_RelatedRailSection` into private `StatelessWidget`s in the same file. | `show_detail_screen.dart` | ✅ DONE — `c73ef055` CI ✅ |
| UX3-09 | Show Detail loads everything behind one global spinner. Break into per-section skeleton shimmer states — hero loads first (image + title), then episode/cast/related sections each shimmer independently so the screen feels faster even when the data load time is identical. | `show_detail_screen.dart` | ✅ DONE — `c73ef055` CI ✅ |
| UX3-10 | True miniplayer (play/pause while browsing) requires a persistent background audio service + global Riverpod playback state. Implemented: `PlaybackService` owns the player after minimize; MX-Player-style background audio while browsing; player HUD gains 12/24h clock + battery indicator. | `mini_player_bar.dart`, `services/playback_service.dart` | ✅ DONE — `5cf5c0e0` CI ✅ (original `76a64295` CI failed; `5cf5c0e0` fixed compile error + 4 runtime bugs) |
| BILLING-FIX-2026-07-14 | `/billing/` crashed with `{"error":"internal error"}` (HTTP 500). Root cause: `received_sms_payments` table was referenced in 3 route files (`payment_gateway.py`, `tid_panel.py`, `db_mgmt.py`) but was never added to `db.py`'s `_DDL` list — table never existed in the live DB, causing SQLite `OperationalError` on every page load. Also found: `payment_methods` table was missing 5 columns used by the save form (`account_name`, `icon`, `min_amount_pkr`, `amount_tolerance_pkr`, `updated_at`). And no POST endpoint existed for the admin phone app to submit incoming SMS data despite the UI having a gateway key for it. Fix: (1) added `received_sms_payments` CREATE TABLE + 2 indexes to `_DDL`; (2) added ALTER TABLE migrations for 5 missing `payment_methods` columns; (3) added default method seeding (EasyPaisa/JazzCash/NayaPay/SadaPay) + auto-generated `sms_gateway_key` in `init_db()`; (4) added POST `/billing/api/sms/receive` endpoint with gateway-key auth + TID auto-matching + configurable auto-approve. Deployed to Oracle via `push_to_oracle.sh`. `GET /billing/ → 200` confirmed in live logs. | `radd-hub/hub/db.py`, `radd-hub/hub/routes/payment_gateway.py` | ✅ DONE — commit `90328920`, deployed to Oracle, verified live |
| AUDIO-PANEL-SAVE-2026-07-14 | Audit of `_openAudioEffectPanel` callbacks in `_ps_ui_mixin.dart`: verified every callback (onEqBandChanged, onEqEnabledChanged, onReverbChanged, onLabAfChanged, onLabStateChanged, onBalanceChanged, onPresetSelected) for `_scheduleSavePrefs()`. One bug found: `onPresetSelected` → `_applyPreset` called directly (no lambda wrapper), applied EQ preset via `_applyAllAf()` but never called `_scheduleSavePrefs()` — preset selection was lost on next launch. Same class as SW Decoder bug. Fix: added `_scheduleSavePrefs();` after `_applyAllAf()` in `_applyPreset()` in `_ps_audiolab_mixin.dart`. All other callbacks confirmed correct; onLabAfChanged noted as fragile (no direct save call) but safe because `_applyLabAf()` always co-fires onLabStateChanged which does save. | `_ps_audiolab_mixin.dart` | ✅ DONE — `5f829ecd` CI ✅ |
| PLAYER-FIXES-2026-07-14 | Full audit of all player feature toggles, color filters, and audio/subtitle properties. Six bugs found and fixed: (1) Vivid Mode color matrix was fake yellow-tint — replaced with proper Rec.709 saturation+contrast matrix. (2) `_toggleSmartEnhance()` missing `_scheduleSavePrefs()`. (3) `onSWDecoderChanged` missing `_scheduleSavePrefs()`. (4) Saved subtitle style (font/size/bold/color/opacity/shadow/alignment/edge-pad/fit) only applied when subtitle panel opened — NOT at startup; added `_applySubtitleStylePrefs()` to `_PlayerSubtitleMixin`, called from `_loadPrefs()` with 700ms delay. (5) `_useSWDecoder` restored to Dart state but `hwdec: no` never sent to MPV — hardware decoding ran regardless of saved pref until codec failure auto-detected; fixed in `_loadPrefs()`. (6) `_currentBalanceAf` was never rebuilt in `_loadPrefs()` — `_audioBalance` state was loaded correctly but the AF pan= string stayed empty so `_applyAllAf()` at startup emitted a centerless pipeline; saved L/R balance had zero effect until the user moved the slider. All other AF strings (reverb, lab, channel mode) were correctly rebuilt. Episode-change audio AF path audited — `af` property persists across MPV loadfile by design, so no re-apply needed on episode nav. | `_ps_ui_mixin.dart`, `_ps_subtitle_mixin.dart`, `player_screen.dart` | ✅ DONE — `04ddc47d` (vivid, CI ✅), `9bc539e8` (SW dec save, CI ✅), `e911085102bb` (sub style+SW dec startup, CI ✅), `1d24b44f` (balance AF startup, CI pending) |
| PROFILE-TEXTFIELD-CONSISTENCY-2026-07-13 | Fixed inconsistent/broken text field styling on the profile edit surfaces. `add_edit_profile_screen.dart`'s Name field was a raw `TextField`+`Container` (no focus-state color change, different padding than the rest of the design system) — migrated to `RaddTextField`. `edit_profile_screen.dart`'s Display Name/Email fields and the Change Password sheet's 3 password fields used a duplicated local `_Field`/`_PwField` implementation instead of the shared component — replaced all 5 with `RaddTextField`, removing ~155 lines of duplicate logic. Extended `RaddTextField` with optional `maxLength`/`textCapitalization` params (needed for the Name fields) so no functionality was lost in the migration. Login/register screens were audited and already used `RaddTextField` correctly — no changes needed there. | `design_system/components/radd_text_field.dart` | ✅ DONE — commit `a316703a` |
| PHASE-J-CI-FIX-2026-07-13 | Found (while verifying CI on the row above) that the **prior** commit `cc150e15` (Phase J2 mixin extraction) had already broken the Android release build: `flutter build` failed with "The setter '_streamError' isn't defined" and same for `_silenceSkipThreshold`, both inside `_PlayerUIMixin` (`_ps_ui_mixin.dart`). Root cause: both fields are real state owned by other mixins (`_ps_playback_mixin.dart` / `_ps_audiolab_mixin.dart`); `_ps_ui_mixin.dart`'s cross-cluster abstract interface only declared a `get` for each, not the `set` needed by two `setState(() => x = v)` call sites inside that mixin — same "abstract declaration must mirror every access, not just reads" gap the J2 handoff notes already flagged for other members. Added the two missing `set` declarations. Grepped all 8 player part-files for the same class of gap (get-only abstract declared but assigned to in the same file) — no other instances found. Not compiled locally (no Flutter SDK) — fix verified via CI. | `raddflix_flutter/lib/screens/player/_ps_ui_mixin.dart` | ✅ DONE — commit `75473077`, CI green; see PHASE-J row for ongoing J3–J6 |
| DOCS-UPDATE-2026-07-12 | Update canonical docs with new rules and corrections: AGENT_HANDOFF.md (Phase H current state replacing stale Phase G In Progress header), AGENT_PROMPT.md (bootstrap step 1 — viewEnvVars code check + token-in-URL clone), RULES.md (Rules 48/50 — secret verification, dual CI check). No code changed. | canonical docs | ✅ DONE — commit pushed 2026-07-12 |
| PHASE-G-2026-07-12 | Phase G of TEN_POINT_PLAN: Architecture Modernisation — now fully closed. G2 done (flutter_staggered_animations + animated_text_kit removed — zero usages; animations/OpenContainer kept). G5 done: AppConstants mutable statics (apiBaseUrl/jazzDriveDeltaUrl/supportWhatsApp) → remoteValuesProvider (Riverpod) + appContainer for non-widget access. G3 done: video_thumbnail → media_kit-based frame extraction (services/media_kit_thumbnail_extractor.dart), package removed from pubspec.yaml. G1 (go_router migration): re-confirmed with user 2026-07-12 to leave the literal package swap undone (unverifiable runtime routing behavior, no device/CI coverage) and mark complete — the actual root cause G1 was chasing (player_screen.dart's 3 `ModalRoute.of(context)?.settings.arguments` reads, the mechanism behind BUG-FREE-PLAY-01) was already fixed via typed PlayerScreen constructor params. G4: user reversed their earlier decision and approved deletion 2026-07-12 — all 13 lettered stub files in core/player/ re-verified zero-import and deleted (see PHASE-G4-DELETE row). Folder reorg of remaining live player files still NOT done — separate, unapproved decision. | `agent-hub/TEN_POINT_PLAN.md` | ✅ FULLY CLOSED — G2/G3/G5 (commits 58a0137, c9974ac, fb8e65d), G1 closed via typed-args fix (commit 2580b88) + marked complete (commit 2f2a11d), G4 deletion done (commit 2f2a11d) |
| PHASE-G4-DELETE-2026-07-12 | Deleted the 13 lettered stub files in `core/player/` (c/d/f/g/n/o/p/q/r/s/t/u/v_series_*) after re-verifying zero imports across `lib/` and getting explicit user approval to reverse the earlier "keep as-is" decision. Also closes I1 (same file list, Phase I). Folder reorganisation of the remaining live `core/player/` files into feature subfolders was NOT part of this approval and is still open if wanted later. | `agent-hub/TEN_POINT_PLAN.md` | ✅ DONE — commit 2f2a11d |
| PHASE-J-2026-07-13 | Phase J of TEN_POINT_PLAN: Player God Class decomposition — now FULLY COMPLETE. J-prep (panel-class extraction) + J2 (`_PlayerPlaybackMixin`) done first (see history below). J3 (`_PlayerAudioLabMixin`, 342 lines), J4 (`_PlayerSubtitleMixin`, 160 lines), and J5 (`_PlayerUIMixin`, 3,285 lines — the largest cluster: gestures, panels/sheets, HUD widgets) were extracted in the same session as J2 but shipped with two cross-mixin compile bugs that were never caught locally (no Flutter SDK here — CI is the only compiler): `_PlayerAudioLabMixin` was missing an abstract `_audioSync` get/set (fixed same session, commit `43d6d76e`), then `_PlayerUIMixin` was missing abstract setters for `_streamError`/`_silenceSkipThreshold` (both real fields owned by sibling mixins) — this broke the Android release build silently until caught while verifying an unrelated later push, fixed 2026-07-13 (commit `75473077`). J6 (slim `_PlayerScreenState`) is satisfied as a side effect: main file is now 1,617 lines (from 9,458 originally), class declares `with WidgetsBindingObserver, _PlayerPlaybackMixin, _PlayerAudioLabMixin, _PlayerSubtitleMixin, _PlayerUIMixin`. **First fully-green CI run of the complete 4-mixin split is commit `7a1b05b0`** — verified via GitHub Actions `build-apk.yml`, not just brace/paren balance checking. Updated `TEN_POINT_PLAN.md` J2–J6 checkboxes to reflect actual code state (they were still unchecked despite the extraction being done). | `agent-hub/TEN_POINT_PLAN.md` | ✅ FULLY DONE — CI green on commit `7a1b05b0` (https://github.com/raddclub/raddflix-app/actions/workflows/build-apk.yml) |
| PHASE-H-2026-07-12 | Phase H of TEN_POINT_PLAN: Testing Infrastructure. H1 done (test/ dir structure + mocktail dep + wired `flutter test` into CI as its own job, which didn't exist before). H4 done (RaddButton/RaddChip/RaddTextField/RaddCard/RaddSheet widget tests). H5 done (PlayerPrefs save/load round-trip test covering one field per settings category + defaults + derived getters). | `agent-hub/TEN_POINT_PLAN.md` | ✅ DONE — commits 809f537, a022e48 |
| PHASE-L-2026-07-12 | Phase L of TEN_POINT_PLAN: Production Hygiene — L1+L2 gate Debug Logs tile+5-tap easter egg behind kDebugMode\|\|isAdmin; L3 replace raw log path in debug_diagnostics_screen; L4a–e friendly exception messages (vault/edit_profile/subscription/admin_queue; L4b/f/L5a–c/L6/L8 verified N/A — stale findings or already fixed); L7 gate NavigatorObserver DebugLogger behind kDebugMode in app.dart; L9 clean BUG-A* comment tags in profile_screen.dart. L10 (ApiClient.isGuestMode → Riverpod authConfigProvider, mirrors G5's remoteValuesProvider/appContainer pattern; reset on logout) done 2026-07-12. | `agent-hub/TEN_POINT_PLAN.md` | ✅ DONE — commits 7035956, 6f27ca0, e65617b, 049dfaf, 7231766, 98c0550 (L10) |
| PHASE-I4-2026-07-12 | Phase I of TEN_POINT_PLAN (I4 only): moved `run_tests.js`, `jazzdrive_logic_test.js`, `verify_links.js` from `raddflix_flutter/test_suite/` to repo-root `scripts/` — plain Node scripts don't belong in a Flutter test dir. Updated `.github/workflows/ci-tests.yml` path, `raddflix_flutter/README.md` and `raddflix_flutter/test_suite/README.md` command references. Dart files (`logic_tests.dart`, `jazzdrive_dart_test.dart`) stay in `test_suite/`. I1 (lettered dead-file deletion) NOT done — same file list as G4, already declined by user; not re-litigated here. | `agent-hub/TEN_POINT_PLAN.md` | ✅ I4 DONE — commit 945c7a6; I1 blocked on prior user decision |
| PHASE-K-2026-07-12 | Phase K of TEN_POINT_PLAN: Performance Polish. K1 already satisfied by B6 (rebuildFtsIndex on background isolate) — no action needed. K4 done: added `fadeInDuration` to the two actor/cast `CachedNetworkImage` call sites (`cast_rail.dart`, `actor_screen.dart`) — the only actor-photo usages in the app. K3 does NOT apply — this app's bottom nav (`RaddFlixBottomNav`) drives `Navigator.pushNamed` between separate routes, not a shared `TabBarView`/`IndexedStack`, so there's no subtree for `AutomaticKeepAliveClientMixin` to attach to; forcing that architecture change just to satisfy K3 would be a real nav-model rewrite, not the small fix the plan estimated. K2/K5 (exhaustive `const`-everywhere audit) NOT attempted: spot-checked the 5 core design-system components and found them already fully const-optimized (zero hits for the common missed patterns); a genuine full-app sweep needs `dart fix --apply` / `flutter analyze`, which this environment can't run (no Flutter SDK) — hand-auditing 67k lines without compiler feedback risks the exact class of breakage this plan exists to prevent. | `agent-hub/TEN_POINT_PLAN.md` | ✅ K1/K4 DONE (commit 0c416ba); K3 N/A by design; K2/K5 deferred — needs Flutter SDK/dart fix |
| PHASE-F-2026-07-12 | Phase F of TEN_POINT_PLAN: Design system migration for remaining 70% of screens (28 tasks) — raw Color/spacing/radius literals → AppColors/RaddSpace/RaddRadius tokens; RaddButton/RaddChip/SettingsRow/RaddTextField component adoption. F01 home_screen: RaddButton+RaddMotion; F02 show_detail: RaddMotion; F03 search: RaddMotion; F04 profile: AppColors.simosaAccent; F05–F07 local screens: RaddMotion; F08 settings: full SettingsRow adoption; F09 login: RaddButton replaces _GradientButton+OTP OutlinedButtons, AppColors.warning; F10 register: RaddButton replaces inline gradient+OutlinedButton; F11 subscription: RaddMotion; F12 edit_profile: RaddMotion; F13 vault: AppColors.simosaAccent; F14 debug_diagnostics: AppColors.success/error + RaddRadius; F15–F17/F20–F24: no exact-match tokens (brand colors/off-scale values kept with comments); F18 history + F19 watchlist: RaddRadius; F25 content_card: AppColors.success/info; F26 simosa_card: AppColors.primary/primaryDark; F27 quick_settings_panel: RaddRadius.smRadius/mdRadius/lgRadius + RaddMotion; F28 player_hud: RaddRadius.smRadius + RaddMotion | `agent-hub/TEN_POINT_PLAN.md` | ✅ DONE — commits b7a26ba, 3cbe121, 7389e61, 9614ee0, 1d91c8ab |
| PHASE-E-2026-07-11 | Phase E of TEN_POINT_PLAN: E1 extracted SyncNotifier (sync_provider.dart) from CatalogNotifier — CatalogNotifier.syncFromServer() now a thin delegate, CatalogNotifier.onSyncComplete() called back from SyncNotifier.sync(); E2 extracted PosterSyncNotifier (poster_sync_provider.dart); E3 moved appNavigatorKey/pendingVideoUri/pendingVideoTitle/pendingSubtitleUri globals in app.dart into Riverpod providers (app_navigation_provider.dart) — required restructuring main.dart to create its own ProviderContainer + UncontrolledProviderScope since those globals are read/written before the widget tree exists; E4 subscription_provider.dart submitTid() now calls loadStatus() (fire-and-forget) on success so plan/quota refreshes without reopening the screen | `agent-hub/TEN_POINT_PLAN.md` | ✅ DONE |
| PHASE-D-2026-07-11 | Phase D of TEN_POINT_PLAN: D1 deleted duplicate lib/widgets/radd_text_field.dart, migrated its 3 importers to design_system RaddTextField; D2 added prefixIcon/keyboardType/validator/maxLines/focusNode to design_system RaddTextField (validator wired via FormField so Form.validate() works); D3 RaddSheet tabbed body AnimatedSwitcher → IndexedStack (preserves tab scroll position); D4 hardcoded hex colors → AppColors tokens in EqVisualizer, QuickSettingsPanel, RaddLockPad; D5 added subtitle/iconColor params to SettingsRow; D6 audited+replaced direct PhosphorIcons calls with AppIcons (bottom_nav.dart; added 2 missing AppIcons entries rather than swap in mismatched glyphs) | `agent-hub/TEN_POINT_PLAN.md` | ✅ DONE |
| PHASE-C-2026-07-11 | Phase C of TEN_POINT_PLAN: C1 _openPanel() helper (7 copy-paste blocks → 1 method) in player_screen.dart, C2 debounce _savePrefs (300ms Timer, ~53 call sites → _scheduleSavePrefs), C3 dead-var audit (re-verified _currentFramedrop and _labDialogueOnly — both have real reads/writes, NOT dead, no removal made), C4 dispose() still calls _savePrefs() directly (cancels debounce timer first) | `agent-hub/TEN_POINT_PLAN.md` | ✅ DONE |
| PHASE-B-2026-07-11 | Phase B of TEN_POINT_PLAN: B1 N+1 episodes, B2 deviceId loop, B3 SUM query, B4 atomic transaction, B5 indexes+version, B6 FTS delay, B7 retry | `agent-hub/TEN_POINT_PLAN.md` | ✅ DONE |
| PHASE-A-2026-07-11 | Phase A of TEN_POINT_PLAN: A1 mounted guard, A2 dead file delete, A3 voice stub, A4 fake KBPS, A5 session leaks ×3, A6 search debounce, A7 RepaintBoundary ×3, A8 build() compute ×3, A9 catalog select | `agent-hub/TEN_POINT_PLAN.md` | ✅ DONE |
| TEN-POINT-PLAN-2026-07-11 | Full codebase audit (22 parallel subagents, every .dart file read — 67,988 lines) + second audit pass (8 parallel grep searches for debug leaks, raw exception exposure, admin gates, TODOs, fake data, revenue bugs). Produced `agent-hub/TEN_POINT_PLAN.md`: 11 phases (A–L), ~95 discrete tasks with exact file + line references. Phases: A critical bugs + session leaks; B DB performance (N+1, no transactions, missing indexes); C player structural fixes (_openPanel helper, _savePrefs debounce, dead vars); D widget layer (duplicate RaddTextField, RepaintBoundary, RaddSheet IndexedStack); E provider architecture (CatalogNotifier God split, globals to Riverpod); F design system migration (remaining 70% of screens, 28 tasks); G architecture (go_router, package consolidation, folder reorg); H testing infrastructure; I dead code removal; J Player God Class decomposition; **L Production Hygiene** (Debug Logs tile shown to all users, DebugDiagnosticsScreen reachable by everyone, 6 screens showing raw e.toString() to users, _friendlyError/AuthErrors raw fallback, revenue leak BUG-A23 _isFree stuck true, navigator logs in production, fake KBPS display). No code changed — plan only. | `agent-hub/TEN_POINT_PLAN.md` | ✅ DONE — plan written, ready for coding agent |
|---|---|---|---|
| SUBTITLE-SYSTEM-FIXES-2026-07-10 | Fixed subtitle-system bugs from prior audit (gzip decode misuse, 3× HttpClient leaks, secondary-sid null-safety crash, unpersisted sub-delay, style desync between the online-subtitle panel and PlayerPrefs, misleading AI-dub error message, CC icon reflecting track existence instead of selection state, OpenSubtitles token re-login on every panel open, preview missing font family). | See `agent-hub/history/2026-07.md` 2026-07-10 SUBTITLE-SYSTEM-FIXES entry | ✅ DONE — commit `1f649878` |
| PROFILE-UX-SCREENSHOT-FIXES-2026-07-11 | Fixed 8 UI/UX issues found via screenshot review of Profile, Edit Profile, Choose Theme, and Home screens: avatar rendering literal "0" for phone numbers starting with the Pakistani "0" trunk prefix, "Active Subscription" banner shown to free users (never checked `isActive`), duplicated phone number display, debounced offline-banner connectivity flicker, ambiguous unlabeled "clear emoji" chip, unclear relationship between Standard/Color theme sections, redundant duplicate Save action, SIMOSA card colors hardcoded instead of using the shared accent token. | See `agent-hub/history/2026-07.md` 2026-07-11 PROFILE-UX-SCREENSHOT-FIXES entry | ✅ DONE — commit `f63fbd07` |
| SIMOSA-LOGO-AND-AUDIO-TRACK-FIXES-2026-07-11 | User supplied the real SIMOSA app icon and reported broken audio-track UX: some videos play with no audio, switching audio track does nothing, and the audio panel shows no indication of which track is active. Added `assets/brand/simosa_logo.jpg`, wired into `SimosaCard` and the quota-full SIMOSA promo (replacing the placeholder "J"/🔥 badge). Root-caused and fixed 3 player bugs — see `agent-hub/history/2026-07.md` 2026-07-11 entry. | See `agent-hub/history/2026-07.md` 2026-07-11 SIMOSA-LOGO-AND-AUDIO-TRACK-FIXES entry | ✅ DONE — commits `caccae5d`, `a2a6b342` |
| SIMPLIFY-AUTH-FLOW | User request: remove genre-selection and multi-step onboarding from login/register. Splash → login directly; login = phone + password only; register = phone + password + confirm only. Dead onboarding route/constants/method all removed. | See `agent-hub/history/2026-07.md` 2026-07-10 SIMPLIFY-AUTH-FLOW entry | ✅ DONE — commits `0c9d2f61`–`eadc104c` |
| PHASE6-ONBOARDING | Replace generic PageView onboarding carousel with Volume V 3-step reciprocity flow (genre taste → starter watchlist → save/signup), per fully-specified plan in AGENT_HANDOFF.md "Phase 6 READY TO BUILD" section. 4 files: constants.dart, onboarding_screen.dart, splash_screen.dart, profile_provider.dart. | `AGENT_HANDOFF.md` Phase 6 section, `agent-hub/UI_UX_MIGRATION_PLAN.md` Phase 6 | ✅ COMPLETE then REVERTED (2026-07-10) — onboarding gate removed per SIMPLIFY-AUTH-FLOW user request |
| UI-UX-AUDIT-2026-07-09 | Full UI/UX & design-system audit of `raddflix_flutter` vs `docs/design-system/`. Read all 17 design-system docs, inventoried token layer, component layer, all 33 screens (29,429 lines) and all 67 widgets. Key finding: token layer 100% compliant, component layer built but 0-1 usages per component (RaddButton/RaddCard: 0 usages app-wide), 31/33 screens untouched by the new component library and still on the legacy `AppColors`/`AppRadius` system or raw literals (410 hardcoded `Color()`, 2,563 `Colors.*`). Output: `docs/AUDIT_UI_UX_REPORT.md` (scorecard, screen-by-screen table, gap analysis, prioritized roadmap, top 20 improvements). No app code changed — audit only. | `docs/AUDIT_UI_UX_REPORT.md` | ✅ DONE |
| MIGRATION-BLUEPRINT-2026-07-09 | Follow-on to the UI-UX audit: a full execution roadmap (not another audit) — release readiness dashboard, screen-by-screen compliance matrix, before/after token mapping table (AppColors→RaddColors, BorderRadius.circular→RaddRadius, EdgeInsets→RaddSpace, raw TextStyle→RaddType, raw Curves→RaddMotion), design-debt inventory with counts, 12 user-journey reviews, UX heuristic evaluation, player-experience deep dive, 8-phase migration roadmap with effort/dependencies/risk, component migration plan, per-screen visual consistency checklist, and beyond-spec modernization ideas. Key structural finding: `AppColors` is the concrete backing store for the `RaddColors` extension (not a competing value system), so most color migration is a mechanical call-site swap, not a value change. Also confirmed `RaddLockPad`/`RaddSpace`/`RaddRadius`/`RaddType`/`RaddMotion` have 0 direct call sites anywhere in screens/widgets. All figures grep-verified against current source, no speculation. Output: `docs/DESIGN_SYSTEM_MIGRATION_BLUEPRINT.md`. No app code changed — planning doc only. | `docs/DESIGN_SYSTEM_MIGRATION_BLUEPRINT.md` | ✅ DONE |
| UI-UX-MIGRATION-PHASE-0..7 | Multi-session execution of the blueprint above. Phased checklist lives in `agent-hub/UI_UX_MIGRATION_PLAN.md` — read that file for the current phase and the first unchecked item; built for hand-off across multiple agent sessions/accounts (each phase's checkboxes are the state, no separate handoff file needed). Phases 2, 3, 4, 5, 6, 7 are complete, CI green on every commit. Phase 0 (Flutter SDK/tooling) not started but no longer blocking. Phase 1 "Player HUD footprint" closed: static-code analysis confirms no 5-control rule violation; live-device pixel measurement deferred indefinitely (no SDK/emulator in env) and accepted as sufficient closure. | `agent-hub/UI_UX_MIGRATION_PLAN.md` | ✅ DONE — all actionable items complete |
| RELIABILITY-PREFLIGHT-2026-07-10 | Added `preflight_check.sh`, a heuristic bash check wired into `auto_commit.sh` (runs automatically before every push touching `.dart` files) to catch the two mistake classes that previously broke CI: a design-token class referenced without its import, and `const SomeStaticClass.field` misused as a constructor call. Aborts the push on either pattern; `SKIP_PREFLIGHT=1` bypasses for false positives. Documented as Rule 47 in `agent-hub/RULES.md` — heuristic only, does not replace CI (Rule 46) since no Flutter SDK is available locally. | `agent-hub/RULES.md` Rule 47 | ✅ DONE — `2cab39c` |
| DS-PHASE2-TOKENS | Design-system Phase 2 (Theme Tokens): added `lib/design_system/{spacing,radius,typography,motion,elevation}/` — `RaddSpace`, `RaddRadius`, `RaddType` (BuildContext extension), `RaddMotion` (wraps existing `AnimConfig`), `RaddElevation` (card/sheet/modal presets + `blurWrap`). Added semantic color getters (`signalPrimary`, `accentDataFree`, `accentWarning`, `accentError`) to `RaddColors` extension + `AppColors.dataFree` constant. 100% additive — no existing files/behavior changed, `AppRadius`/`AppColors`/`AppDurations` kept as-is for backward compat. Ready for Phase 3 (`RaddButton`, `RaddCard`, `RaddSheet`, etc.) to consume. | `docs/design-system/07-developer-guidelines.md` Build Order + `ROADMAP.md` Phase 2 | ✅ DONE — `da38249` |
| DS-PHASE3-COMPONENTS | Design-system Phase 3 (Shared Components): added `lib/design_system/components/` — `RaddButton`, `RaddCard`, `RaddSheet` (+ no-stacking guard), `RaddBanner` (+ priority map), `RaddTextField`, `RaddChip`, `RaddLockPad`, `SettingsRow`, matching the Volume IV/VIII contracts. Wrote the 4 missing Volume VIII contracts (`RaddChip`/`RaddLockPad`/`SettingsRow`/`RaddTextField`) into `08-component-api-contracts.md`. 100% additive — not wired into any screen yet. **Caveat: no Flutter SDK available in this Replit environment (only Dart-Tools module, no Flutter), so these were never compiled or widget-tested — only manually brace/paren balance-checked.** Marked 🚧 In Progress (not ✅ Production) in the Component Status Dashboard until a real Flutter build/test pass confirms them, per Volume VII testing requirement. | `docs/design-system/04-components.md`, `08-component-api-contracts.md` | ✅ DONE — see commit below, ⚠️ needs Flutter-toolchain verification before Phase 4 screen work consumes these |
| PLAYER-DEAD-CODE-CLEANUP | Follow-up to PLAYER-DOCS-CORRECTION. Re-verified 2026-07-08 via full import trace across `lib/` + test suite: 50 files in `raddflix_flutter/lib/widgets/player/` are confirmed dead code (never imported anywhere) — corrected from the original 47-file list (`binge_guard.dart` was mis-classified as "keep", moved to dead; `picture_profiles_sheet.dart` + `film_grain_overlay.dart` were missing, added). Full corrected list in `docs/design-system/09-migration-guide.md` "Player — corrected 2026-07-08" section. **User reviewed the full list and declined to delete anything** — several look like intentionally-parked unshipped features (Sleep Timer, Cast, Zoom/Crop, etc.), not confirmed junk. **This is a closed decision, not an open task.** Do not re-propose deletion or re-derive this inventory without new instruction from the user — read the corrected list in the two docs above first. | `docs/design-system/09-migration-guide.md` + `IMPLEMENTATION_PLAN.md` (both corrected 2026-07-08) | ✅ CLOSED — re-verified, corrected, user declined deletion (no code deleted) |
| DS-AUDIT-2026-07-08 | Full docs-vs-reality audit: compared every design-system volume against the actual `raddflix_flutter` code (token layer, 8-component library, Home/Search/Show-Detail/Player/Settings screens, accessibility, modal discipline, Reciprocity first-run flow). Findings: token layer + component library exist and mostly match spec (RaddTextField already live in login/register/subscription screens, corrected in `07-developer-guidelines.md` dashboard — was wrongly listed as 0 screens); `RaddMotion` Pulse/Tune values don't match spec (1200/800ms easeOutCubic vs. documented 1800ms/200ms spring — needs a decision, not yet fixed); all 5 screens are 0% migrated (matches `ROADMAP.md` Phase 4-9, expected); Player specifically has **active rule violations** beyond "not yet migrated" — 12-14 persistent HUD controls (target 5+More), panels open at 55% width (target ≤40%), video is explicitly paused when panels open (target: never blocked), 4s auto-hide (target 3s); Reciprocity principle (Volume I) is violated — `splash_screen.dart` sends unauthenticated users straight to login, no browse-before-signup path exists. Full detail in `docs/design-system/CHANGELOG.md` 2026-07-08 "Full docs-vs-reality implementation audit" entry. No code changed — audit only. Follow-up decisions needed before acting: (1) align `RaddMotion` values to spec or update the spec to match the tuned values, (2) confirm whether fixing the Player's HUD-count/40%-width/non-blocking-playback/auto-hide violations should happen now (independent bug-style fixes) or be folded into PLAYER-CONSOLIDATION, (3) explicit user sign-off before touching the Reciprocity/auth-gating flow, since it changes real login behavior, not just UI styling. | `docs/design-system/CHANGELOG.md` (2026-07-08 audit entries), `07-developer-guidelines.md` (corrected dashboard) | ✅ DONE — mechanical fixes implemented (motion tokens, 3s auto-hide, ≤40% panel width); non-blocking-playback finding was a false positive, corrected; HUD-count consolidation (12-14→5+More) deliberately deferred to `PLAYER-CONSOLIDATION` — same scope, needs a dedicated pass with a Flutter toolchain available to compile/test; Reciprocity/auth-gating change explicitly declined by user for now |
| PLAYER-CONSOLIDATION (RaddSheet) | Migrate all 7 real inline player panels to `RaddSheet.show`. Steps 1–3 done (commits d9da81f, dae6c3c, 663f475): VideoZoomPanel removed + _openZoomPanel → RaddSheet.show; AudioTrackPanel header stripped + _openAudioPanel → RaddSheet.show; QuickShortcutsPanel header stripped + _openMoreMenu → RaddSheet.show. Steps 4–7 (commit 777df5a): SubtitlePanel, AudioEffectPanel, SettingsPanel, SidebarCustomizerPanel — headers stripped (title row + back-button removed, tab rows kept); all 4 call sites converted from _openRightPanel() to RaddSheet.show(style: list, maxHeightFraction: 0.90). Bounded-height handled naturally by RaddSheet's ConstrainedBox — panels with Expanded children work because RaddSheet gives finite maxHeight. _openRightPanel() now has no callers in main player flow (legacy landscape path preserved intact). | `docs/design-system/IMPLEMENTATION_PLAN.md` sections 2+3 | ✅ DONE — `777df5a` (⚠️ CI in progress — verify run 28964526692 succeeds before next Flutter-touching work) |
| DOWNLOAD-TAB-V2 | 5-tab bottom nav (Home/Search/Local/Download/Profile, drops merged-Library idea); redesign Download tab UX for simplicity (Netflix/MoviBox/1DM+/Amazon Prime/Snaptube/MX Player research); Download tab gets Movies section (flat) + TV Shows section grouped by show → per-season folders → episodes | See `agent-hub/DOWNLOAD_TAB_REDESIGN_PLAN.md` | ✅ DONE — `094fd7d` |
| I1-LAB-FEATURES | Add 4 new Audio Lab features: Dialogue Only (centre-channel sum isolates speech), Night Audio (acompressor tames explosions), Stereo Widener (extrastereo=m=2.5), Noise Reduction (afftdn=nf=-25). Also fixed vocal-remover 0.5 scale inconsistency in `_loadPrefs` AF rebuild. | `player_screen.dart` — 7 touch points: state vars, loadPrefs restore, loadPrefs AF rebuild, savePrefs, onLabStateChanged call site + widget props, panel class typedef/fields/ctor, state class vars + _applyLabAf + initState + UI toggles | ✅ DONE — `6e4da14` |
| J1-PLAYER-BUGFIX | Full player audit (66 working, 5 partial bugs, 1 dead-code cast). Fixed all 5 bugs: (1) silence-skip _silenceInPipeline not restored from prefs, (2) Vivid/SmartEnhance not persisted, (3) _showSkipBtns not persisted, (4) _showPrevNextBtns not persisted, (5) Layout "compact" had no real visual effect — now applies tighter padding + shorter transport row. | `player_screen.dart` — _loadPrefs, _savePrefs, _buildBottomArea, _buildTransportRow | ✅ DONE — `cd75b25` |
| J2-SAVE-PREFS-FIX | Code-trace verification found 4 more bugs: _toggleSmartEnhance() missing _savePrefs() call (only saved on dispose), onShowSkipBtnsChanged / onShowPrevNextBtnsChanged / onShowSeekPositionChanged callbacks also missing _savePrefs(). _showSeekPositionLabel had no prefs key at all. Fixed all 4 + added pref_seek_pos_label. | `player_screen.dart` — _toggleSmartEnhance, _openSettingsPanel callbacks, _loadPrefs, _savePrefs | ✅ DONE — `26232d7` |
| K1-SAVE-ON-PAUSE | Save watch position immediately on pause event (playing stream listener). Previously only saved on 10s timer, background lifecycle, dispose, and episode change — a force-kill while paused lost up to 10s. One line added inside playing.listen when v=false. Frame thumbnails checked: not in code, not created per instruction. | `player_screen.dart` — playing.listen callback | ✅ DONE — `5408c98` |
| Portrait-Player-V1 | Fix video player portrait mode layout — YouTube/Netflix split (video top 38% + controls panel below), compact top bar, bottom-sheet settings panel, portrait-aware gesture zone, fix hardcoded offsets | `e851819`, `c1131ce` | ✅ DONE |
| Portrait-Player-V2 | 7 portrait layout issues fixed: 16:9 exact video zone (no black dead-band), sidebar suppressed in portrait, persistent always-visible back button, new clean `_buildPortraitTransportRow` (no Lock/Immersive/Settings clutter), `spaceEvenly` controls panel (no dead space on tall phones), `SingleChildScrollView` removed, Lock moved to quick actions row | `896ec06` | ✅ DONE — `896ec06` |
| BUG-FREE-PLAY-01 | Free content wrongly showed subscription paywall on online play (worked fine after download). Root cause: `onGenerateRoute`'s `PageRouteBuilder` for `AppRoutes.player` never forwarded `settings:`, so `ModalRoute.of(context)?.settings.arguments` inside `PlayerScreen` always resolved to null — `is_free` was silently lost on every online play, treating all streamed content as paid. | `app.dart` — added `settings: settings` to the player `PageRouteBuilder` | ✅ DONE — `a9d90c5` |

---

## 🛡️ Hard Rules for Every Animation Phase (ANIM-*)

> An agent MUST verify these before marking any ANIM task as DONE:
> 1. ✅ Gated behind `AnimConfig.tier` check
> 2. ✅ Respects `MediaQuery.disableAnimations`
> 3. ✅ No `BackdropFilter` on API < 28 (Tier < 2)
> 4. ✅ No fragment shaders on API < 26 (Tier < 2)
> 5. ✅ `RepaintBoundary` on every isolated animated widget
> 6. ✅ All controllers/listeners disposed in `dispose()`
> 7. ✅ Tested on API-21 emulator — must not crash or jank
> 8. ✅ Duration ≤ 350ms on Tier 0/1

---

## Completed Work Index

Full detail for every row below (root cause, code diffs, testing notes) lives in
`agent-hub/history/TASK_LOG.md` — use the phase name or commit SHA to find it fast.

| Phase / Batch | Summary | Commit(s) | Status |
|---|---|---|---|
| Phase 41 | Performance infra — AnimConfig tiers, animation packages, RepaintBoundary audit | `8396c13` | ✅ DONE |
| Phase 42 | Hero poster transition (home/search → detail) | `50717ac` | ✅ DONE |
| Phase 43 | Staggered grid/list entry animations | `4f55fcd` | ✅ DONE |
| Phase 44 | Card → detail morph via OpenContainer | `2600a39` | ✅ DONE |
| Phase 45 | Neon/glow primary action buttons | `bec1909` | ✅ DONE |
| Phase 46 | Typewriter & animated text (synopsis, chips) | `647ac5c` | ✅ DONE |
| Phase 47 | Frosted glass bottom nav | `af27e1a` | ✅ DONE |
| Phase 48 | 3D tilt hero banner | `a8d4323` | ✅ DONE |
| Phase 49 | Ambient particle background (splash/login) | `f81b0cb` | ✅ DONE |
| Phase 56 | Subscription tier badge (animated glow) | `a34b5f9` | ✅ DONE |
| Audit Fixes | Guest/Free/Premium episode lock logic | `336dbb5` | ✅ DONE |
| 5-Feature Batch | Settings screen, search history, subtitle picker, update check, continue-watching | `1859ec1` / `a6d938b` / `2562512` / `d2f8146` | ✅ DONE |
| Phase 57 | Player audit + fix: dual subtitles (secondary-sid), fake track bugs, EAC3/DTS auto-fallback, codec badges, MKV embedded subtitle selector | audit: N/A · impl: see `TASK_LOG.md` "Phase 57 Implementation" | ✅ DONE |
| Phase 58 | Online subtitle search overhaul (OpenSubtitles XML-RPC, manual search, language chips) | see `TASK_LOG.md` "Phase 58" | ✅ DONE |
| Phase 60 | Remove-dub / dub-active indicator in Audio Panel | `803f09a` | ✅ DONE |
| Build Fix | Kotlin 2.2.0 bump (flutter_tts 4.2.5 compat) | `a8e5bf1` | ✅ DONE |
| Build Fix | minSdkVersion 21 → 24 (flutter_tts 4.2.5 requirement) | `98323a8` | ✅ DONE |
| Full-App Audit | 15 verified bugs fixed across 7 files (dispose leaks, mounted guards, static regex, OOM guard, etc.) | see `TASK_LOG.md` "Full-App Bug & Logic Audit" | ✅ DONE |
| Bug-Fix Batch 2026-07-02-B | Dub visibility, ASS subtitle margin override, free-user `isFree` bool parsing | `b0b01ff` / `7835605` | ✅ DONE |

---

## Repo Consolidation & Resilience Work (2026-07-03)

| Task | Summary | Commit(s) | Status |
|---|---|---|---|
| Docs consolidation | Archived 15 stale handoff docs, rewrote `AGENT_PROMPT.md` as single safe entry point | `5618f33` | ✅ DONE |
| Script hardening | `push_to_github.sh` / `push_to_oracle.sh` — `set -euo pipefail`, no token-to-disk, branch/merge guards, secret-leak guards, DRY_RUN | `7fed76c`, `c93d075` | ✅ DONE |
| Live push test | Verified real commit+push+revert in isolated `/tmp` clone; fixed lock-file-gets-committed bug | `a56d12c` | ✅ DONE |
| `agent-hub/OPERATIONS.md` | Full connect/edit/push guide for GitHub + Oracle | `1a656f7` | ✅ DONE |
| Bootstrap section | `AGENT_PROMPT.md` now walks a fresh session through secret checks + doc order before waiting for a task | `3c52e22` | ✅ DONE |
| `agent-hub/RESILIENCE.md` | Scaling/fallback playbook — local sub-agents vs. Project Tasks, fallback ladders, verify-before-success, hard boundary on safety | `c0cec01`, `62e46ad` | ✅ DONE |
| Oracle drift fix | Server was 281 commits behind + UU conflict markers + local edits. Backed up server files, `git checkout -f origin/main`, `git pull --ff-only`. Server now at `baf349f`, clean tree, healthz ✅ | — | ✅ DONE 2026-07-03 |
| Bug-Fix Batch 2026-07-03 | Free-content play gate + download failures: episode `is_free` inheritance in all 3 catalog API endpoints; movie/episode play gates bypass for local files; episode download URL decode; retryDownload URL decode; `_isSubExpired` live provider | `8176835` | ✅ DONE |
| MPV-Native Player Upgrades 2026-07-03 | Native `ab-loop-a/b` (removed Dart-side seek polling), background next-episode link prefetch for near-gapless transitions, screenshot-with-subtitles (long-press) | `4cda21c` | ✅ DONE |
| Full Audit Pass 2026-07-03 | Subtitles, player controls, access control, downloads — see `TASK_LOG.md` "Full Audit Pass 2026-07-03" | see below | ✅ DONE |

---

## Auto-Commit Workflow (2026-07-04)

| Task | Summary | Commit(s) | Status |
|---|---|---|---|
| Auto-commit system | `auto_commit.sh` — lightweight GitHub API commit script; Rule 42 added to RULES.md; AGENT_PROMPT.md updated | `a0d2d9f` | ✅ DONE |
| Icon migration — gesture_map_sheet.dart | Replace 2× `Icons.block_rounded` with `AppIcons.block`; add `app_icons.dart` import — completes 100% AppIcons coverage across non-player files | `3ec3c81` | ✅ DONE |
| Test suite — complete run | All 4 test files executed: JS logic 27/27, Dart logic 69/69, Integration 71/71, JazzDrive Dart 0/8 (MED-1011 — Oracle session expired, not a code bug). SSL cert fix applied to `jazzdrive_dart_test.dart` for Nix/Replit Dart | `2ce6ab4` | ✅ DONE |
| Correct wrong info — TTL & validationkey | Fixed: `constants.dart` TTL 180min→110min; `jazzdrive_dart_test.dart` validationkey direction inverted (was claiming vk MUST be in URL, production says DO NOT add); `logic_tests.dart`/`run_tests.js`/`README.md`/`local_db.dart` stale 6h/180min references | `5d37e39`, `19fa4cf` | ✅ DONE |
| Logger secret stripping — final gap closed | `DebugLogger.logApi()` req/resp/error bodies now redacted via `_redactBody()` (validationkey, k= tokens, JSESSIONID, Authorization/Bearer, access_token/refresh_token) before truncation/storage; verified no unredacted secret patterns reach any log call | `015bcea` | ✅ DONE |
| Rebrand zero-rating copy (hide JazzDrive) | `subscription_screen.dart` "JazzDrive CDN" line now attributes zero-rating to RaddFlix; `debug_diagnostics_screen.dart` "Oracle Server"/"JazzDrive API" check labels renamed to "Content Server"/"Stream Service", `JAZZDRIVE` log filter renamed to `STREAM`; Jazz SIM requirement copy and SIMOSA/JazzCash/Easypaisa payment copy left untouched; global grep confirms zero JazzDrive/Oracle occurrences in any `Text()`/`SnackBar`/label visible to users | `f3f6453` | ✅ DONE |

---

## UI Audit & CI Fix (2026-07-05)

| Task | Summary | Commit(s) | Status |
|---|---|---|---|
| CI fix — PhosphorIcons.wifi | `app_icons.dart`: `PhosphorIcons.wifi()` → `PhosphorIcons.wifiHigh()` — icon renamed in phosphor_flutter 2.1.0; was the sole compile error blocking APK builds | `a4b946b` | ✅ DONE |
| UI audit — gate Server Downloads | `AppUser` model: add `isAdmin` field reading `is_admin` from JWT/JSON (defaults false). `profile_screen.dart`: "Server Downloads" row now guarded by `user?.isAdmin == true` instead of `user?.isGuest != true` — was visible to all paying users | `1e511a0` | ✅ DONE |
| UI audit — remove hardcoded prices | `tid_status_screen.dart`: plan label getter replaced `'Basic (₨149/month)'`/`Standard`/`Premium` with `'Basic Plan'`/`'Standard Plan'`/`'Premium Plan'` — hardcoded prices caused dispute risk if backend prices change | `22d1a93` | ✅ DONE |
| UI audit — fake→decoy wording | `vault_settings_screen.dart`: two user-visible strings changed from "fake vault / fake PIN" to "decoy vault / decoy PIN" — internal variable names unchanged | `ebf74b6` | ✅ DONE |
| Flask is_admin field | `db.py`: added `is_admin INTEGER DEFAULT 0` to `app_users` DDL + idempotent migration. `mobile_api.py` `/me`: SELECT + JSON response now include `is_admin`. `user.dart`: `AppUser.isAdmin` field + `fromJson` parse. Deployed to Oracle. ⚠️ SQL grant pending: phone `03257719165` has no row in `app_users` yet — user must log in once to create the row, then run `sqlite3 /opt/jazzmax/radd-hub/data/radd_hub.db "UPDATE app_users SET is_admin=1 WHERE phone='03257719165';"` via SSH. | — | ✅ DONE — `4498fa4d` (SQL grant pending) |

---

## Bug Fix Batch — Free/Paid Gate, Subtitle Panel, AI Dub, Misc (2026-07-05)

| Task | Summary | Commit(s) | Status |
|---|---|---|---|
| D1 — subtitle download broken | `player_screen.dart` `_downloadOnlineSubtitle`: `'\${dir.path}/\$cleanName'` — backslash escapes kill interpolation, file written to literal path `${dir.path}/…`. Fix: remove backslashes. Same on snackbar line. | `71d9cb7` | ✅ DONE |
| A1 — BUG-FREE-PLAYER | `player_screen.dart` `_openMediaForEpisode` only reads `ep['is_free']` — ignores show-level `isFree`. Fix in `show_detail_screen.dart` `_playEpisodeImpl`: normalize every episode in `allSeasonsEps` to `is_free=1` when `widget.item.isFree==true` before passing to player. | `6f39440` | ✅ DONE |
| A2 — BUG-RESUME-MOVIE | `resume_fab.dart` `_play`: `?? false` default when `resume_is_free` key absent (old installs). Fix: treat absent key as `true` (free) for movies already in prefs — no key means it predates the BUG-11 write; safer to let them through than lock them out. | `55a2ab8` | ✅ DONE |
| B1+B2 — dub panel blocks settings | `player_screen.dart` `_SubtitlePanel`: AI Dub section pinned ABOVE tabs consumes ~213 dp — hides all settings in landscape. Fix: move into its own `AI Dub` tab; show tab only when `onDubRequested != null`. | `3d6cb2f` | ✅ DONE |
| C1 — TTS langResult=0 not caught | `subtitle_dubber.dart`: `langResult < 0` misses `0` (LANG_COUNTRY_AVAILABLE with no data). Add preflight: synthesize short string, check output file is non-empty. | `3d6cb2f` | ✅ DONE |
| C2 — TTS preflight test | `subtitle_dubber.dart`: after `setLanguage()`, synth `'test'` to temp file; if absent or zero bytes → `LANG_NOT_INSTALLED`. | `3d6cb2f` | ✅ DONE |
| C3 — TTS path fallback | `subtitle_dubber.dart`: try `getExternalStorageDirectory` then `getApplicationDocumentsDirectory` for clip lookup; already done in existing code. | — | ✅ DONE (pre-existing) |
| C4 — TTS better error snackbar | `player_screen.dart`: C1+C2 preflight now catches the failure early with `LANG_NOT_INSTALLED` status, so the existing prompt logic fires correctly without additional changes needed. | — | ✅ DONE (via C1/C2) |
| C5 — In-panel TTS install hint | `player_screen.dart` `_buildDubSection`: added amber hint row with "Install" tappable action (deep-links to Android TTS settings via `android_intent_plus`) shown at top of AI Dub section before any dub buttons. Users see the install prompt up-front before attempting a dub. | `1b257467` | ✅ DONE |
| E1 — BUG-POSTER-WIPE | `local_db.dart` `upsertTitle`: `poster_path` missing from insert map — wiped to NULL on every full sync. Fix: add `'poster_path': item.posterPath`. | `6f39440` | ✅ DONE |
| E2 — BUG-IS_NEW-CAST | `catalog_item.dart`: `json['is_new'] as bool?` throws TypeError when server sends integer. Fix: `json['is_new'] == true \|\| json['is_new'] == 1`. | `6f39440` | ✅ DONE |
| E3 — BUG-MIXED-SEASON-DL | `show_detail_screen.dart` `_downloadCurrentSeason`: blanket gate blocks free episodes in mixed seasons. Fix: filter queue to free-only when unsubscribed instead of early-return. | `aa6d60c` | ✅ DONE |
| E4 — BUG-RESUME-MOUNTED | `show_detail_screen.dart`: cross-season resume uses `Future.microtask` — fires before rebuild, `_currentEpisodes` still old season, always returns -1. Fix: `addPostFrameCallback` + `mounted` guard. | `aa6d60c` | ✅ DONE |
| E5 — BUG-DL-ICON-DEAD-TERNARY | `show_detail_screen.dart` `_EpisodeTile`: `onDownload != null ? null : null` — disabled icon same color as enabled. Fix: use `t.textSecondary.withOpacity(0.35)` for disabled branch. | `aa6d60c` | ✅ DONE |
| E6 — BUG-SUB-NOT-REACTIVE | `show_detail_screen.dart`: added `ref.watch(authProvider)` + `ref.watch(subscriptionProvider)` at top of `build()` — PREMIUM lock badges now rebuild immediately when subscription activates. | `aa6d60c` | ✅ DONE |
| E7 — BUG-EPISODE-BADGE | `show_detail_screen.dart`: episode badge now uses `ep['episode']` actual number instead of list position `realIdx`. | `aa6d60c` | ✅ DONE |
| E8 — BUG-GUEST-CTA | `show_detail_screen.dart` `_requireSub`: guest now routed to `AppRoutes.login` instead of `AppRoutes.subscription` (which led to paywall+register). Label kept 'Sign In' as this now correctly routes to login. | `aa6d60c` | ✅ DONE |
| F1–F6 — zero-rated text cleanup | Removed/replaced JazzDrive brand text: subscription subtitle, `_JazzPartnerBadge` widget + class, `jazzSavingsMsg` plan card block, `_WhyRaddFlix` jazz item, onboarding slide, settings tile. | `2ccc373` | ✅ DONE |

---

## Adding new work here

| PROFILE-FIELD-FOCUS + PLAYER-LANDSCAPE-PANELS | (1) `edit_profile_screen.dart`: `_Field` → StatefulWidget with FocusNode; animated icon-bg tint 8%→15% primary, animated label color muted→primary, label size 12pt, icon radius `RaddRadius.smRadius`. (2) `player_screen.dart`: `_openRightPanel` barrier opacity 0.38→0.12, panel bg solid dark surface; all 7 panel openers (`_openSubtitlePanel`, `_openAudioPanel`, `_openZoomPanel`, `_openAudioEffectPanel`, `_openMoreMenu`, `_openSidebarCustomizer`, `_openSettingsPanel`) now check orientation at open — landscape routes to `_openRightPanel(panel, widthFactor)`, portrait keeps existing RaddSheet. | See `agent-hub/history/TASK_LOG.md` 2026-07-10 PROFILE-FIELD-FOCUS entry | ✅ DONE — commit `72f93a8d` |
| BACKEND-AUDIT-2026-07-14 | Full backend audit: 10 bugs fixed across 8 Python + 2 Flutter files; `proxy_pool_page.py` deleted (orphaned). Python: registered `bp_catalog_secure` blueprint (was missing → all `/api/catalog/*` 404); fixed `get_catalog_share_url` SQL; fixed watch-history column names (`position_sec/duration_sec/updated_at` → `position_ms/duration_ms/watched_at`); added `APP_VERSION_KEYS` tuple (NameError) + fixed `overview IS NOT NULL` stat query → `plot IS NOT NULL`; added `app_signatures` DDL; fixed `catalog_fts`→`titles_fts` + dropped columns in `db_mgmt.py`; analytics `NULL as name`→`COALESCE(u.device_name,'')` ; poster proxy key-rotation fixed (WHERE clause was comparing plaintext against Fernet-encrypted column — never matched). Flutter: `home_screen.dart` `/api/config`→`/api/app/config`; added missing `ApiPaths.adminQueue` constant. Deployed to Oracle. CI failure on `737d956` was GitHub Actions infra (corrupted SDK zip), not code — re-run passed green. | `5861ff5` (Python), `737d956` (Flutter), `54cb17a` (deletion) | ✅ DONE — CI ✅ |
| UX-BATCH-2 (Tasks 7-9) — Empty states + transitions + miniplayer | `actor_screen.dart` empty state now uses `AnimatedSearchIcon` (was static icon), matching Search/Downloads/Local Media. `watchlist_screen.dart`/`history_screen.dart` grids gained the tier-gated `OpenContainer` shared-element morph already used on Home/Search. New `mini_player_bar.dart` (`MiniPlayerBar`/`MiniPlayerDock`) replaces the Home-only `ResumeFab` FAB with a persistent continue-watching bar docked above the bottom nav on all 5 top-level screens — reuses the same `resume_*` SharedPreferences keys; no live background-playback service exists in this app so this is a resume bar, not an actively playing/pausable miniplayer. | `b7b74c20` | ✅ DONE — CI ✅ |

| VERSION-BUMP-1.1.0 + SESSION-HANDOFF | Comprehensive launch-readiness audit. Verified: Oracle on `45dc75f4` ✅, 84 live channels serving ✅, all CI secrets set ✅, build #1730 green ✅, login flow correct ✅, all blueprints registered ✅, no blocking code bugs. Bumped `pubspec.yaml` 1.0.0+3 → 1.1.0+4 to mark the Live TV + advanced player + phonetic subtitles + theme redesign milestone. Updated `AGENT_HANDOFF.md` to reflect Oracle deploy complete (logo patches applied). | ✅ DONE | 2026-07-22 | `b39050b2` CI #1731 ✅ |
| LIVE-P0 | **Critical: fix live stream resolution — all live TV 100% broken.** `_openMedia()` routed live channels through `JazzDriveService.getStreamLink()` → `_extractShareKey()` threw on CDN URLs → `_friendlyError()` matched "Jazz" in the exception string and showed a false "Jazz SIM required" error. Fix: `widget.contentType=='live'` early-exit opens `widget.streamUrl` directly (bypasses JazzDrive entirely); live-specific `_friendlyError()` clause; auto-retry 30s → 10s for live. Note: `_isLive` getter is defined in `_ps_ui_mixin.dart` — playback mixin must inline `widget.contentType=='live'`. | `_ps_playback_mixin.dart` | ✅ DONE — `aa997d82` CI ✅ |
| LIVE-P5 | **Live TV tab: FEATURED badge → design-system red pill.** Amber `Color(0xFFFFC107)` badge inconsistent with `AppColors.primary` (#C41E3A) red used everywhere else. One-line colour swap + text colour to white. Both banner pill and grid star badge updated. | `live_tv_screen.dart` | ✅ DONE — `cfe0fe9b` (CI ✅ via `aa997d82`) |

1. Add a row with ⏳ IN PROGRESS *before* starting.
2. When done and pushed, flip to ✅ DONE and fill in the commit SHA — verify the SHA is real by
   re-fetching it from GitHub, don't just paste what the push command echoed.
3. Write the full detail (root cause, files touched, testing notes) into
   `agent-hub/history/TASK_LOG.md`, not into this file — this file stays a one-line-per-item index.
4. If a task is blocked or intentionally not automated (e.g. needs human judgment, touches
   production), mark it ⚠️ OPEN with a one-line reason instead of silently dropping it.

| M1 — FLASK-RULE38-TEMPLATES | Replaced all 20 `confirm()`/`prompt()` calls with arm+fire toast pattern across 9 Flask templates: `admin.html`, `scan.html`, `library.html`, `bots.html`, `db_mgmt.html`, `home.html`, `stream.html`, `proxy_pool_page.html`, `_proxy_pool_panel.html`. Rule 38: Cloudflare blocks `confirm()`/`prompt()` on radd-hub. | `fb6992f` | ✅ DONE |
| M2 — FLASK-RULE38-UPLOAD | Two `confirm()` calls missed in M1: `deleteFlixAccount()` and `deleteAccountById()` in `upload.html`. Same arm+fire pattern; per-id arm key for `deleteAccountById` so multiple rows arm independently. | `1ddbceb` | ✅ DONE |
| N1 — PROFILE-ESCAPED-DOLLAR + KDEBUGMODE | (1) `profile_screen.dart`: `'v\${info.version}'` showed literal `v${info.version}` in UI; `'\$e'` in two catch blocks logged literal `$e` (same escape bug as L1 audio labels). Fixed: remove backslashes. (2) Gated all 6 bare `debugPrint()` calls across `profile_screen.dart`, `subscription_screen.dart`, `word_dict.dart`, `player_screen.dart` with `if (kDebugMode)` (Rule 21). | `49a3b8e` | ✅ DONE |
| N2 — LOCALDB-SUBDUBBER-KDEBUGMODE | Gated final 2 bare `print()` calls in `local_db.dart` (stream-cache cleanup log) and `subtitle_dubber.dart` (synthesis error log) with `if (kDebugMode)`. Added `flutter/foundation.dart show kDebugMode` import to both files. All Rule 21 violations now 100% resolved across codebase. | `db54b45` | ✅ DONE |
| O1 — FLASK-VALUEERROR-400 | All 35 `int(request.args.get())` calls raised unhandled `ValueError` → 500 on malformed params. Added global `@app.errorhandler(ValueError)` in `app.py` returning 400 JSON. | `a2943fd` | ✅ DONE |
| O2 — POSTER-PUSH-STOP-AUTH | `POST /api/catalog/poster-push/job/<job_id>/stop` had no auth guard. Job IDs are Unix timestamps (guessable). Added `_check_admin_auth()` inline check (→ 401) matching `poster-push/bulk` pattern. | `20765be` | ✅ DONE |
| O3 — OTP-BRUTE-FORCE-GUARD | `POST /api/auth/device-switch/verify` had no attempt counter — 1M combinations bruttable in 10-min OTP window. Added `_otp_attempts` dict (phone-keyed): 5 wrong guesses burns OTP + returns 429; correct guess clears counter. | `bcbd41f` | ✅ DONE |
| P1 — APK-BUILD-BREAK-FIX | N1 regression: `if (kDebugMode)` guards added to `player_screen.dart`/`subscription_screen.dart` without importing `flutter/foundation.dart` — broke last 2 CI APK builds silently. Added missing import to both files; verified via fresh successful workflow run producing `RaddFlix-1.0.0+3-build1488.apk`. | `8fd8fdf` | ✅ DONE |
| G1 — SUBTITLE-PANEL-REWRITE | `player_screen.dart` `_SubtitlePanelState`: fix `sub-back-color` hex format, Box shadow, dual opacity, 5-tab layout, live preview, `_toMpvColor`/`_toMpvBackColor` helpers, `_SubTrackTile`+`_BigStepBtn`. | `778f17c` | ✅ DONE |
| G2 — SETTINGS-PANEL-POLISH | `player_screen.dart` `_SettingsPanelState`: progress bar style chips (Wrap), Controls/Navigation card sections with icon rows. | `778f17c` | ✅ DONE |
| H1 — AUDIO-LAB-BUGFIX | `player_screen.dart` `_AudioEffectPanel`: A1 logging, A2/A3 merged EQ (allMatches sum, no double-equalizer), A4 vocal-remover 0.5× scale, A5 MPV sync on open, A6 verified, Lab EQ coupling fix. Final commit: `9ee224d`. | `9ee224d` | ✅ DONE |
| L1 — AUDIO-PANEL-INTERP + SUB-STYLE-PERSIST | (1) Audio track labels showed literal `${widget.tracks[i].language}` — escaped `\${}` in double-quoted strings. Fixed to single-quoted proper interpolation. (2) Subtitle style/position settings (11 vars: font, size, bold, color, bg, opacity, shadow, alignX, alignY, edgePad, fitToVideo) were never saved or restored — reset to defaults on every panel open. Added `_loadSubPrefs()`+`_saveSubPrefs()` to `_SubtitlePanelState`; prefs loaded+re-applied to MPV in initState; `_saveSubPrefs()` called from every onChanged handler (13 call sites). | `7d2c77e` | ✅ DONE |
| L2 — PORTRAIT-VOLUME-INDICATOR + SW-DECODER-PERSIST | (1) Portrait volume indicator rendered at `left:20` — same side as brightness indicator — causing both to overlap. Volume swipe is right-half gesture so indicator must be at `right:20`. Fixed. (2) `_useSWDecoder` toggle in Audio Panel had no SharedPreferences save or load — reset to `false` every session. Added `pref_sw_dec` to `_loadPrefs` + `_savePrefs`. Full panel persistence audit confirmed: all other panels (EQ, reverb, channel mode, speed, lab, silence-skip, sidebar order) are in main `_PlayerScreenState` and ARE correctly persisted — only subtitle style (L1) and SW decoder (L2) had this issue. | `bb324c1` | ✅ DONE |
| L4 — SYNC-RESET + SUBSPEED-PERSIST + SAVEPREFS-GAPS | (1) `_subSync`/`_audioSync` were never reset on episode change — MPV `sub-delay`/`audio-delay` persist across `loadfile`, so episode N's sync offset bled directly into episode N+1. Fixed: `_subSync = 0.0`, `_audioSync = 0.0`, `_subSpeed = 1.0` in `_playEpisodeAt` setState block + `_np.setProperty('sub-delay','0')`, `audio-delay`, `sub-speed` in MPV reset block. (2) `_subSpeed` had no prefs key — reset to 1.0 on every app restart. Added `pref_sub_speed` to `_loadPrefs`/`_savePrefs`. Added `_savePrefs()` to `onSpeedChanged` callback. (3) `_showRemainingTime` seek-bar tap never saved. (4) Settings panel: `onShowRemainingChanged`, `onKeepScreenChanged`, `onSkipIntervalChanged`, `onSeekSwipeSpeedChanged` all missing `_savePrefs()` — values only persisted on dispose (lost on force-kill). All four fixed. | `68562849` | ✅ DONE |
| L5 — ZOOMMODE-SAVEGAP | `_openZoomPanel` callback: `setState(() => _zoomMode = mode)` but `_savePrefs()` was never called — zoom mode preference was lost on every restart. Added `_savePrefs()` call. | `d9922a1` | ✅ DONE |
| L6 — MPV-STARTUP-RESTORE | `_loadPrefs` restored `_subSpeed` and `_videoRotation` into Dart state but never pushed to MPV — user saw correct value in UI but MPV ran at wrong rotation/speed until they touched the control manually. Added `setProperty('sub-speed',...)` + `setProperty('video-rotate',...)` guarded by `!= default` check. `_zoomMode` verified Flutter-only (BoxFit) — no MPV property needed. | `16de119` | ✅ DONE |
| L3 — BUG-AUDIO-CARRY-01 + LANGUAGE-PREF-PERSIST | (1) BUG-AUDIO-CARRY-01: `_playEpisodeAt` reset `sid`/`secondary-sid` to auto (BUG-SUB-CARRY-01 fix) but never reset `aid` or `_selectedAudio` — audio track ID carried over to next episode's different track list, matching wrong language. Fixed: `_selectedAudio = null` + `_np.setProperty('aid','auto')` added to episode-change block. (2) Language preference not persisted: user's explicit subtitle/audio track language choice was lost on every episode change and restart. Added `_prefSubLang`/`_prefAudioLang` state vars + `pref_sub_lang`/`pref_audio_lang` prefs keys. Saved when user explicitly picks a track (only if `language` field non-empty). Re-applied via `Future.microtask` in `stream.tracks.listen` after each new file's track list arrives — selects first matching-language track if available, else leaves MPV's auto-selection unchanged. Also added `_savePrefs()` call to both `onSubtitleTrackSelected` and `onTrackSelected` audio callbacks (neither previously saved). | `b896632` | ✅ DONE |
| S1 — PHASE3-TOKEN-PASS | Phase 3 color+radius token pass across 6 auth/small screens: `login_screen.dart`, `register_screen.dart`, `watchlist_screen.dart`, `history_screen.dart`, `splash_screen.dart`, `settings_screen.dart`. Replaced `AppColors.primary/error/warning` with `context.signalPrimary/accentError/accentWarning`; `BorderRadius.circular(AppRadius.sm/md)` with `RaddRadius.smRadius/mdRadius`. Added `radd_colors.dart` + `radd_radius.dart` imports to each. Handled const-TextStyle conflicts. Unmapped tokens (`warningDark`, `primaryGradient`, `info`, `success`) intentionally kept. **CI fix (2026-07-09):** Prior session introduced `context.signalPrimaryGradient` (does not exist on BuildContext) in `login_screen.dart` and `register_screen.dart` — caused 2 CI failures. Fixed in `4ee0215` by replacing with `AppColors.primaryGradient` (correct pattern used everywhere else in the codebase). CI build confirmed green on `4ee0215`. | `4ee0215` (CI fix) | ✅ DONE |
| R1 — PHASE2-CONTENTCARD-TOKEN-PASS | `content_card.dart`: added `import radd_radius.dart`; replaced all 6 `BorderRadius.circular(AppRadius.sm/md)` calls with `RaddRadius.smRadius` / `RaddRadius.mdRadius`. First design-system radius token adoption in the widget layer (AppRadius.sm=10→RaddRadius.sm=8, AppRadius.md=14→RaddRadius.md=12 — intentional design normalization per blueprint). CI queued on commit `6457ab2`. | `6457ab2` | ✅ DONE |
| Q1 — DOCS-CORRECTNESS-PASS | Full re-verification session found: Oracle was 1 commit behind `main` (P1 fix never redeployed) — fixed by re-running `push_to_oracle.sh` against final HEAD, confirmed matching SHA + live smoke-tested O1/O2/O3 against the real server. Also found stale/contradictory doc guidance (`AGENT_PROMPT.md` said use normal `git push`; `RULES.md`/`PROJECT_RULES.md` said no git commands at all — reconciled to: read-only git locally is fine, actual push is always via `auto_commit.sh`). Added Rules 43-46 to `agent-hub/RULES.md` (kDebugMode import requirement, Oracle non-auto-deploy + final-HEAD redeploy, dead `hub/` dir at repo root vs real `radd-hub/hub/` — also present on the Oracle server itself, always verify CI run status after Flutter pushes). Mirrored fixes into `.agents/PROJECT_RULES.md` Rules 1/3/5. | docs-only | ✅ DONE |

## Phase 5 continuation (2026-07-09)

| Task | Summary | Commit(s) | Status |
|---|---|---|---|
| V1 — PHASE5-VAULT-SCREEN-TOKEN-PASS | `vault_screen.dart` radius token pass (2× `BorderRadius.circular(12)`→`RaddRadius.mdRadius`) | `b550de0` | ✅ DONE |
| V2 — CI-FIX-BATCH-REGRESSION | Prior batch commit (`45a5f1cd`, not this session) broke CI: missing `AppColors` import in `player_settings_screen.dart`/`layout_designer_screen.dart`, invalid `const AppColors.error` (not a constructor) in `profile_switcher_screen.dart`. Fixed all three; CI build `64ec457` confirmed green. | `64ec457` | ✅ DONE |
| V3 — PHASE5-VAULT-SETTINGS-TOKEN-PASS | `vault_settings_screen.dart` radius token pass: `BorderRadius.circular(12)`→`RaddRadius.mdRadius`, `BorderRadius.circular(8)`→`RaddRadius.smRadius` (2 exact matches). Kept 10/6/2/14 (off-scale). CI `787255a` confirmed green. | `787255a` | ✅ DONE |
| V4 — PHASE5-SEASON-FOLDER-TOKEN-PASS | `season_folder_screen.dart` color+radius token pass: `Color(0xFF22C55E)`→`AppColors.success` (4×, exact match); `AppRadius.md/sm`→`RaddRadius.mdRadius/smRadius` (normalization, same pattern as Phase 3). Kept `AppRadius.round` (pervasive elsewhere, out of scope) and `BorderRadius.circular(3)` (off-scale). CI `1290f80` confirmed green. | `1290f80` | ✅ DONE |
| V5 — PHASE5-EDIT-PROFILE-TOKEN-PASS | `edit_profile_screen.dart` radius token pass: only `BorderRadius.circular(12)` (`_PwField`) matched scale → `RaddRadius.mdRadius`. Kept 9/10/13/14/2 (off-scale). CI `9ca788a` confirmed green. | `9ca788a` | ✅ DONE |
| V6 — PHASE5-ADD-EDIT-PROFILE-REVIEW | `add_edit_profile_screen.dart` reviewed — no exact-scale radius/color literals found (all 9/14/2/20-vertical, off-scale); no changes needed. | n/a | ✅ DONE (no-op) |
| V7 — PHASE5-PROFILE-SWITCHER-TOKEN-PASS | `profile_switcher_screen.dart` radius token pass: `BorderRadius.circular(8)` (KIDS badge)→`RaddRadius.smRadius`. Kept 2/14 (off-scale). CI `6d4e7c1` confirmed green. | `6d4e7c1` | ✅ DONE |
| V8 — PHASE5-TID-STATUS-TOKEN-PASS | `tid_status_screen.dart` radius token pass: `BorderRadius.circular(14)`→`RaddRadius.mdRadius` (2×), `circular(12)`→`RaddRadius.mdRadius` (3×). Kept `circular(10)` (off-scale) and `Color(0xFF25D366)` (WhatsApp brand color, intentional). CI `4b0eab9` confirmed green. | `4b0eab9` | ✅ DONE |
| V9 — PHASE5-ADMIN-QUEUE-TOKEN-PASS | `admin_queue_screen.dart` radius token pass: `BorderRadius.circular(AppRadius.md)`→`RaddRadius.mdRadius` (2×, normalization). Kept `circular(4)`/`circular(3)` (off-scale). CI `d1e1093` confirmed green. | `d1e1093` | ✅ DONE |
| V10 — PHASE5-ACTOR-SCREEN-TOKEN-PASS | `actor_screen.dart` radius token pass: `BorderRadius.circular(AppRadius.md)`→`RaddRadius.mdRadius` (2×, normalization). CI `e3be886` confirmed green. | `e3be886` | ✅ DONE |
| V11 — PHASE5-EXPIRED-QUOTA-TOKEN-PASS | `plan_expired_screen.dart` `circular(14)`→`RaddRadius.mdRadius`; `quota_full_screen.dart` `AppRadius.md`→`RaddRadius.mdRadius` (3×, normalization). Kept `circular(5)` (off-scale). CI `77a10f7` confirmed green. | `77a10f7` | ✅ DONE |
| V12 — PHASE5-LAYOUT-DESIGNER-TOKEN-PASS | Confirmed `screens/player/layout_designer_screen.dart` is the live copy (imported by `app.dart`); `screens/layout_designer_screen.dart` is dead/unreferenced code. Radius pass: `circular(8)`→`RaddRadius.smRadius` (2×). Kept `circular(2)`/`circular(20)` (off-scale). CI `d893d8c` confirmed green. | `d893d8c` | ✅ DONE |
| W1 — UX3-10-CI-FIX-AND-BUG-AUDIT | `76a64295` (UX3-10 miniplayer) broke `build-apk.yml`: `_minimizePlayer()` called from `_PlayerUIMixin` without the required abstract cross-cluster declaration in `_ps_ui_mixin.dart`. Added it. Full logic/UX audit of the 8-file UX3-10 diff also found: usage/billing heartbeat and resume-position saves stopped the instant a session was minimized (both timers lived only on the disposed screen State, not the surviving `PlaybackService`); `WatchPartyService.leaveRoom()`/`VoiceCommandsService.stop()` fired unconditionally on minimize too. Fixed by adding equivalent heartbeat + position-save timers to `PlaybackService` (started in `adopt()`, stopped in `detachForReattach()`/`_disposeCurrent()`), and gating the watch-party/voice/usage-timer dispose-time side effects behind `!_handedOffToService`. Also added a confirm dialog before `MiniPlayerBar`'s stop button (previously ended a live session with one un-confirmed tap) and dimmed the new minimize icon relative to Back so the two adjacent, differently-consequential top-bar controls aren't visually identical. CI `5cf5c0e` confirmed green. | `5cf5c0e` | ✅ DONE |
| W2 — PLAYER-FORMAT-COVERAGE-AUDIT | Audit: does the player play every audio track and video format? Playback engine (media_kit/mpv + bundled ffmpeg) already decodes essentially any container/codec and audio-track switching (`_AudioTrackPanel`, language-pref re-apply, EAC3/DTS/TrueHD SW-decoder fallback) was already correct — no engine-level gap. Found the real gap in three separate hand-maintained Dart extension allowlists (`VaultFile.isVideo`, Vault folder-import filter, `local_media_service.dart` filesystem fallback scan) that only recognized ~10 containers each and silently treated everything else (`.mpg/.mpeg/.vob/.m2ts/.mts/.ogv/.divx/.asf/.rm/.rmvb/.mxf`, etc.) as not-a-video — `VaultFile.isVideo` in particular gated whether tapping a file in the Vault opened the player at all. Unified all three into one `AppConstants.playableVideoExtensions` set covering every container mpv/ffmpeg actually demuxes. CI `2d419f4` confirmed green. | `2d419f4` | ✅ DONE |
| Y1 — PLAYER-VAULT-BUGFIX-BATCH | 5 bugs reported from APK testing: (1) duplicate replay/reload icon in player header, (2) BG Audio shortcut missing from sidebar, (3) vault add shows no feedback on file-not-found / no progress, (4) vaulted file still visible in file managers, (5) restore always goes to Downloads + creates duplicate. | ✅ DONE | 2026-07-16 | `459244b5` CI ✅ |
| Y2 — VAULT-LOGIC-AUDIT | Deep audit of vault implementation vs MX Player approach. Found 5 additional bugs: sidecar writing wrong paths for FilePicker temp-cache, orphan restoreFile not cleaning sidecar, deleteFromMediaStore return value silently ignored (no user feedback when Android 11+ permission dialog dismissed), totalVaultSize counting .raddmeta files, misleading notifyMediaStore(vaultPath) no-op comment. | ✅ DONE | 2026-07-16 | `4da50433` CI ✅ |
| X1 — MUSIC-TAB-LOCAL-MEDIA | Separate Music section on Local Files tab (MX Player style). (1) `MediaStorePlugin.kt`: add `queryAudio()` querying `MediaStore.Audio.Media` + `getAlbumArt()` via album art content URI + request both `READ_MEDIA_VIDEO`+`READ_MEDIA_AUDIO` on API 33+. (2) `LocalVideo` model: add optional `artist`/`album`/`albumId` fields. (3) `LocalMediaService`: add `checkAudioPermission()`, `queryAllAudio()`, `getAlbumArt()`. (4) `local_media_screen.dart`: tabbed Videos\|Music layout — Videos tab shows only video/mixed folders; Music tab shows flat track list with album art, artist/album metadata, inline sort pills (Recent\|Title\|Artist\|Album\|Duration), search, and plays into `PlayerScreen` with full queue. | `84ea9bb` | ✅ DONE |
| Z1 — AUDIO-MODE-CONTROLS-AND-EMBEDDED-ART | Complete the audio-mode music player: (1) Add `flutter_media_metadata` package and extract embedded ID3/Vorbis/MP4 cover art for files without a sidecar image — feeds into disc, backdrop, and palette accent. (2) Add Prev/Next skip buttons flanking play/pause in the glass card — greyed out (opacity 0.35) when no prev/next track. (3) Add Shuffle/Repeat toggle row above seek bar — active state tinted with accent colour. (4) Add `_shuffleEnabled` + `_toggleShuffle()` + `_randomEpIdx()` to playback mixin; wire shuffle into auto-advance countdown. (5) Thread all callbacks through `_ps_ui_mixin.dart` injection site. (6) CI fix: AGP 8.1 `package=` strip in AndroidManifest patch + cross-cluster abstract decls + `ImageProvider?` type fix. | ✅ DONE | 2026-07-16 | `32355924` CI ✅ |
| AA1 — BG-AUDIO-UX-FIXES | Three genuine remaining bugs after re-audit of live code: (1) `bgaudio` absent from default `_sidebarOrder` — button defined but unreachable via sidebar until user manually re-orders. (2) Top-bar `♫ BG` badge was read-only and hidden when BG audio is off — no 1-tap way to turn it on without sidebar or one-handed strip. (3) Notification / lock-screen shows only app launcher icon, not content artwork. Fixes: add `bgaudio` as first item in `_sidebarOrder`; replace static badge with an always-visible tappable headphones icon button in the top bar (orange when active, dimmed outline when off); add `EXTRA_ARTWORK_URL` to `PlaybackService.kt`, fetch poster URL in a background thread on first receipt, refresh notification once bitmap loads; thread poster URL through `_notifyBgState()` and `_startOrUpdateNativeBg()`. Bugs 3/4/5/6/7 from original list confirmed already implemented in current HEAD. CI fix: `_RaddIconBtn.color` is non-nullable `Color`; prior commit passed `null` in one-handed strip — substituted `Colors.white`. CI `f083c3aa` ✅. | ✅ DONE | 2026-07-18 | `f083c3aa` CI ✅ |
| BB5 — FAB-THUMBNAIL-FIX | ResumeFab and MiniPlayerBar show a blank poster card. Audit SharedPrefs key names between `PlayerScreen._saveResumeFab()` write path and `ResumeFab`/`MiniPlayerBar` read paths. Align any mismatched keys. Add `CachedNetworkImage` `errorWidget` fallback (dark grey container + `PhosphorIcons.filmSlate`) so the card never looks broken even if the URL fails. Full spec: `agent-hub/PLAYER_UX_BB_PLAN.md` §BB5. | ✅ DONE | 2026-07-19 | `5fa870fa` + CI fix `255ffe61` CI ✅ |
| BB2 — TTS-LANG-FIX | `flutter_tts setLanguage()` returns LANG_MISSING_DATA even when Google TTS is installed — voice data packs are a separate download. Fix: (1) replace `setLanguage()` availability check with `getVoices()` filtered for target locale + `isInstalled:true`; (2) call `setEngine('com.google.android.tts')` first; (3) try locale variants (`hi-IN`, `hi_IN`, `hi`); (4) fix preflight `synthesizeToFile` to use `getApplicationDocumentsDirectory()` (always writable, no permissions needed); (5) replace raw `'LANG_NOT_INSTALLED'` error with a warm `RaddOverlay.confirm`-style card with a deep-link button to `ACTION_INSTALL_TTS_DATA`. Full spec: `agent-hub/PLAYER_UX_BB_PLAN.md` §BB2. | ✅ DONE | 2026-07-19 | `69b2f5f6` CI fix `c9179df3` CI ✅ |
| BB1 — RESUME-STRIP | Replace `showDialog(AlertDialog)` in `_restoreWatchPos` with auto-resume + non-blocking `_ResumeStrip`. Video starts immediately at saved position. Strip appears above seek bar for 4s: `"▶ Resumed from 12:34  Restart ↺"`. Tap Restart → seek(0) + dismiss. Wait/tap strip → dismiss. Slide+fade entry 180ms easeOutCubic (potato: fade only). No dialog, no blocking. Full spec: `agent-hub/PLAYER_UX_BB_PLAN.md` §BB1. | ✅ DONE | 2026-07-19 | `a2236304` CI fix `c9179df3` CI ✅ |
| BB6 — RADD-OVERLAY-SYSTEM | Create `lib/core/ui/radd_overlay.dart` with three static methods backed by OverlayEntry (works everywhere, no Scaffold dependency): `RaddOverlay.snack()` — bottom pill, slide+fade up, auto-dismiss 3s; `RaddOverlay.confirm()` — bottom card with scrim, stacked buttons, replaces AlertDialog; `RaddOverlay.toast()` — centered pill, scale+fade, 2s, no action. All tier-aware (AnimConfig), all use RaddTheme tokens, all call HapticService. Migrate these call sites: `MiniPlayerBar._confirmStop`, snackbars in `_ps_playback_mixin`, `resume_fab`, and TTS error display (BB2). Full spec: `agent-hub/PLAYER_UX_BB_PLAN.md` §BB6. | ✅ DONE | 2026-07-19 | `41927adc` CI fix `c9179df3` CI ✅ |
| BB3 — SUBTITLE-PRESET-PICKER | Wire orphaned `SubtitlePresetPicker` widget into `_SubtitlePanel` Style tab. (1) Place picker at top of Style tab. (2) When preset ≠ `custom`: collapse all manual sliders. (3) When `custom`: show full sliders (existing behavior preserved). (4) Add `broadcast` preset to `kSubtitlePresets`: bold white, thick black outline (3.0), zero bg, 22sp. (5) Add accessibility font chip row below picker for `SubtitleFont` selection. Full spec: `agent-hub/PLAYER_UX_BB_PLAN.md` §BB3. | ✅ DONE | 2026-07-19 | `1c4a8342` + `6bc88639` CI fix `c9179df3` CI ✅ |
| BB4 — PIP-MINIMIZE-WIRING | Wire the player back/minimize button to trigger `_enterPiP()` when a session is active. Add `pipOnMinimize` bool pref in `layout_prefs.dart` (default true) with a toggle in Player Settings. Show a one-time toast `"Video continues in a floating window"` (2s, gated on `seen_pip_tip` SharedPrefs bool). Falls back to existing mini-bar behavior when pref is off. Full spec: `agent-hub/PLAYER_UX_BB_PLAN.md` §BB4. | ✅ DONE | 2026-07-19 | `6bc88639` CI fix `c9179df3` CI ✅ |
| BB7 — CONTROLS-SLIDE-FADE | Upgrade controls show/hide from opacity-only to slide+fade. Top bar: SlideTransition Offset(0,-1)→(0,0). Bottom controls: Offset(0,1)→(0,0). Both keep existing AnimatedOpacity. Show: 180ms easeOutCubic. Hide: 140ms easeIn. Two new AnimationControllers in `_ps_ui_mixin.dart`, wired to existing `_controlsVisible`. Tier gate: basic+ only (potato = existing opacity). Durations added to `anim_durations.dart`. Full spec: `agent-hub/PLAYER_UX_BB_PLAN.md` §BB7. | ✅ DONE | 2026-07-19 | `42d04167` + `6bc88639` CI fix `c9179df3` CI ✅ |
| BB8 — SUBTITLE-CROSSFADE | Wrap subtitle text widget in `AnimatedSwitcher` with `FadeTransition` 150ms in `subtitle_overlay.dart` and `dual_subtitle_overlay.dart`. Flutter overlay only — MPV-rendered ASS subtitles are unaffected (`sub-ass-override: force` path untouched). Tier gate: potato = instant swap (existing behavior). Full spec: `agent-hub/PLAYER_UX_BB_PLAN.md` §BB8. | ✅ DONE | 2026-07-19 | `657c7374` CI fix `c9179df3` CI ✅ |
| BB-AUDIT — AMBIENT-QUALITY-CHECK | Standing rule that applies to EVERY BB task: while implementing each change, audit the 3–5 adjacent features and screens most affected by that change. Check: (1) UI elements use RaddTheme/RaddRadius tokens, not legacy AppColors/AppRadius; (2) logic actually achieves what the feature claims — do not assume it works, verify; (3) no broken user paths or dead ends exist in the surrounding flow; (4) any animations use AnimConfig tier-awareness; (5) fix minor issues inline; report larger issues as new sub-tasks. Goal: every BB commit leaves the surrounding code measurably better than it found it. Not a separate commit — runs as part of each BB task commit. | 🔁 Runs with every BB task | 2026-07-19 | — |
| BB10 — WORD-LOOKUP-UPGRADE | Upgrade the existing partial word dictionary feature (241 words, single-tap, Urdu-only, no video pause, no context sentence, no audio). Builds entirely on existing: `word_dict.dart`, `word_definition_sheet.dart`, `subtitle_overlay.dart`. Changes: (1) Online fallback via `dictionaryapi.dev` (free, no key) + MyMemory translation API when word not in offline dict; session-level cache via `Map<String,WordEntry?>` in `WordDict`; (2) `dictTargetLanguage` pref (default `'ur'`) + language picker in quick settings — user can set any ISO 639-1 target; update toggle label to language-agnostic text; (3) video pause on word tap via `onPausedForLookup` callback, auto-resume 800ms after dismiss; (4) show actual subtitle context sentence at top of popup with tapped word highlighted; (5) pronunciation audio — prefer `dictionaryapi.dev` `.mp3` URL, fallback to `flutter_tts`; (6) popup animation — scale from word position (easeOutBack 220ms, tier-gated); (7) RaddTheme tokens replacing legacy `AppColors.surface` / hardcoded `#D4784A`; (8) saved vs known word underline distinction (solid = saved, dotted = in dict but not saved); (9) "My Words" 7th tab in subtitle panel — sorted list, search bar, tap to review, long-press to delete, empty state; (10) extend dict support to `dual_subtitle_overlay.dart`. Full spec: `agent-hub/PLAYER_UX_BB_PLAN.md` §BB10. | ✅ DONE | 2026-07-19 | `729bbccb` |
| DA-2 — WATCH-INTEGRITY-SMC | Silent background enforcement: (1) Completion threshold guard (`realPlaySecs ≥ 0.70 × totalDurationSecs`; deny on abuse velocity ≥ 4× AND seek jump ≥ 40%); (2) `UsageService.applySmcIfNeeded` with per-title/per-day cooldown; (3) `smc_log` table + migration v24 (`catalogDbVersion` 23→24); (4) Session tracking fields in `_PlayerPlaybackMixin`; (5) Wired into `dispose()` + `_openMediaForEpisode()` + `_onVideoCompleted()`. Audit fixed 3 bugs: cooldown-on-no-charge, missing backgrounding SMC, reattach `_realPlaySecs` seed. | ✅ DONE | 2026-07-20 | `12f8baae` |
| AB1 — NEO-PHONOGRAPH-AUDIO-PLAYER | Overhaul `audio_mode_backdrop.dart` (993 lines) with Neo-Phonograph aesthetic: (1) Physics spin-down — disc decelerates over 900ms on pause using `Curves.decelerate` (`_spinDownCtrl`), accelerates on play via `_spinUpCtrl` 600ms; tier-gated basic+; (2) Tonearm — new `_Tonearm` CustomPaint widget with brushed-brass gradient, pivot cap, headshell, needle dot; swings in/out 500ms easeInOut when playing/paused; potato = hidden; haptic on settle; (3) Groove rewrite — variable-pitch rings (2.8px at label, 5.6px at edge), label zone (inner 36%) with warm amber fill, label-edge transition ring, alternating groove depth opacity, outer bevel ring; rotating sheen arc (38° soft light sweep) on standard+ only; (4) Spindle cap — `_SpindleCap` CustomPainter with machined bevel + dark metallic body + accent center dot (replaces plain dark circle); (5) Pause overlay — remove opacity dim, add dark circular overlay (0→0.30) + custom pause bars at 0.60 opacity on pause; disc stays full opacity always; (6) Glow pulse sync — `_pulseCtrl.duration` 1600ms playing / 3400ms paused, opacity range playing=0.12+v*0.18 / paused=0.06+v*0.09; (7) Track entrance animation — `_entryCtrl` 400ms easeOutBack disc scale 0.88→1.0 + cover art fade; basic+; (8) Warm vignette — replace `Colors.black` with `Color(0xFF0D0905)` in backdrop gradient; (9) Default accent warm amber `Color(0xFFD4943A)` replacing cold purple; (10) Controls card: seek bar gradient fill, seek thumb pulse while dragging, timestamp micro-animation slide+fade, play button haptic check. Full spec: `agent-hub/PLAYER_UX_BB_PLAN.md` §AB1. | ✅ DONE | 2026-07-19 | `3bd6f011` |

---

## 10/10 Plan — Path from 7.9 to 10 (2026-07-20)

> Derived from the fully-informed revised audit (supersedes `AUDIT_UXCOMPETITIVE_2026.md`).
> Note: P0-A (Audio Lab bugs) = H1 above — already ✅ DONE (`9ee224d`). Starting from P0-B.
> Work order: P0-B → P1-A → P1-G → P1-C → P1-D → P1-B → P1-E.

| Task | Description | Status | Date | Commit |
|---|---|---|---|---|
| P0-B — PHASE-A-STATUS-GREP | Grep audit of `quick_settings_panel.dart` + `player_prefs.dart` for all Phase A–D feature wiring. **Findings:** A1 accent color picker ✅ wired (`color_picker_sheet.dart` 231 lines, `showColorPicker` called in Style tab). A2 seek bar styles ✅ wired (`seek_bar_painter.dart` 408 lines, all 10 styles + `SeekBarStylePreview` strip). A3 button shapes ✅ wired (buttonShape picker in QSP). A3 icon packs ✅ wired (`icon_packs.dart` 199 lines, 6 packs). A5 player skins ✅ wired (`player_theme.dart` 8 built-in themes: RaddFlix Amber / Midnight Purple / Sakura🌸 / Gold Class / Matrix Green / Ocean Cyan / Sunset Orange / Snow White). D1 picture profiles ✅ wired (`picture_profiles_sheet.dart` 245 lines, 6 built-in presets, `onOpenPictureProfiles` callback). C1 gesture remapping ✅ wired (`gesture_map_sheet.dart` 284 lines, `_showGestureMapSheet` in `_ps_ui_mixin.dart`). **Only gaps: IDEA-06 (Subtitle Personality) + IDEA-08 (Phonetic Overlay) — not yet built.** Feature roadmap score actual: 9/10, not 6/10. | ✅ DONE | 2026-07-20 | — |
| P1-A — PLAYER-THEME-ENGINE | Phase A (A1 accent color + A2 seek bar styles + A5 skins): **Already fully built and wired** — confirmed by P0-B grep. `color_picker_sheet.dart`, `seek_bar_painter.dart`, `player_theme.dart`, `icon_packs.dart` all exist and are wired into `quick_settings_panel.dart` Style tab and `_ps_ui_mixin.dart`. 8 bundled themes apply accent + seekBarStyle + buttonShape + iconPack in one tap. | ✅ DONE | 2026-07-20 | pre-existing |
| P1-G — BUTTON-ICON-STYLE | Phase A3 (ButtonShape + IconPack): **Already fully built and wired** — confirmed by P0-B grep. `icon_packs.dart` implements 6 icon packs (mx/ios/fluent/material3/cute/minimal). ButtonShape picker wired in `quick_settings_panel.dart` (line 1410). All stored in `player_prefs.dart`. | ✅ DONE | 2026-07-20 | pre-existing |
| P1-C — SUBTITLE-PERSONALITY | IDEA-06: Subtitle content-aware visual style adaptation. ALL CAPS text → bold + 110% size; `...` → italic + 70% opacity fade; `[whispering]` / `[quietly]` → 80% size italic + 60% opacity; `?!` / `!!` → scale-bounce anim (easeOutBack, tier-gated); `♪` / `🎵` → gradient background pill. Toggle in Subtitle Panel → Style tab + intensity slider (0–100%). **Files:** `subtitle_personality.dart` (new helper, pure Dart), `subtitle_overlay.dart`, `dual_subtitle_overlay.dart`, `player_prefs.dart`. | ✅ DONE | 2026-07-20 | 2640559b |
| LIVE-TV-TAB | Built Live TV tab (4th bottom-nav item, index 1 between Home and Local). New files: `lib/data/live_channels.dart` (86-channel catalogue, `LiveChannel` model, `kLiveCategories`), `lib/screens/live_tv_screen.dart` (category filter chips + search bar + 2-column animated grid, pulsing LIVE badge, `CachedNetworkImage` logos, launches `AppRoutes.player` with `stream_url`). Modified: `bottom_nav.dart` (Live tab at index 1), `home_screen.dart` (LiveTvScreen in IndexedStack, DebugLogger updated), `app_icons.dart` (added `liveTvFill`). Preflight clean. | ✅ DONE | 2026-07-22 | `2fd409f0` |
| LIVE-TV-BACKEND | Backend for Live TV: `live_channels` SQLite table (DDL + 3 indexes) added to `_DDL` in `db.py`. Seed: 84 channels across 7 categories (sports/religious/news/entertainment/kids/movies/docs) seeded once on first boot (`COUNT(*) = 0` guard). New `routes/live_channels.py` — admin panel (`GET /live/`) with category filter tabs, toggle is_active/is_free/featured per channel, inline stream-URL edit (Enter to save), sort-order edit; `?cat=` preserved through all POST redirects. Mobile API `GET /api/live/channels` → JSON for Flutter. Both blueprints registered in `app.py`. Nav link "📺 Live TV" added under APP section in `base.html`. Bug-fixed and deployed to Oracle (`e234e512`). | ✅ DONE | 2026-07-22 | `e234e512` |
| THEME-REDESIGN — OBSIDIAN-CRIMSON | Replace "Warm Hearth" default theme (muddy brown-black bg `#130F0C`, muted terracotta primary `#D4784A`, warm cream text `#F5EFE6`) with Obsidian Crimson: near-black neutral bg `#0D0D0F`, cardinal red primary `#C41E3A`, crisp near-white text `#F8F8FA`. Updated `AppColors`, `AppGradients`, `RaddTheme.dark`, `BrandThemeState.defaults` (both `const` defaults and `reload()` fallbacks). Light/AMOLED/variant themes unchanged. | `constants.dart`, `radd_theme.dart`, `brand_theme_provider.dart` | ✅ DONE | 2026-07-20 | `cd8fc253` |
| P1-D — PHONETIC-SUBTITLE-OVERLAY | IDEA-08: Show Roman Urdu (Romanization) transliteration as a smaller line below Arabic-script / Urdu / Hindi subtitle text. Script auto-detected via Unicode block range (Arabic U+0600–U+06FF, Devanagari U+0900–U+097F). Offline character-map table — no ML, no network. Toggle in Subtitle Panel → Text tab. Unique to Pakistani market; no other player has this. **Files:** `phonetic_subtitle.dart` (new), `subtitle_overlay.dart`, `dual_subtitle_overlay.dart`, `player_prefs.dart`. | ✅ DONE | 2026-07-20 | `bbda2466`→`da9a77e9` |
| P1-B — PICTURE-PROFILES | Phase D1: **Already fully built and wired** — confirmed by P0-B grep. `picture_profiles_sheet.dart` (245 lines) implements 6 presets (Natural / Cinema / Vivid / Night / Anime / AMOLED Saver) with brightness/contrast/saturation/hue values. Wired in player via `onOpenPictureProfiles` callback. Persisted in `player_prefs.dart`. | ✅ DONE | 2026-07-20 | pre-existing |
| P1-E — GESTURE-REMAPPING | Phase C1: **Already fully built and wired** — confirmed by P0-B grep. `gesture_map_sheet.dart` (284 lines) defines `kGestureActions` map + `_ActionPicker` UI. Wired into player via `_showGestureMapSheet` in `_ps_ui_mixin.dart` (line 3140), opened from Quick Settings → Controls tab (line 2819). | ✅ DONE | 2026-07-20 | pre-existing |
| LIVETV-P1 — SCREEN-LAYOUT | Featured hero banner (featured channel, backdrop color gradient, Watch Now button); Netflix-style horizontal category rows in "all" view; 2-col grid in single-category view; backdrop color tinted cards; free/paid lock icon. Added `isFree` to `LiveChannel` model + local DB v26 migration. Files: `live_tv_screen.dart`, `live_channels.dart`, `local_db.dart`, `constants.dart`. | ✅ DONE | 2026-07-22 | `b55d9f55` |
| LIVETV-P2 — PLAYER-LIVE-UI | Live-specific player UI: hide seek bar (replaced with red ● LIVE status row); simplified transport (skip/replay/prev/next hidden); channel switcher button in top bar → bottom sheet channel list → pushReplacementNamed. File: `_ps_ui_mixin.dart`. | ✅ DONE | 2026-07-22 | `ca12dd14` |
| LIVETV-P3 — RECENTLY-WATCHED | Recently watched row at top of Live TV screen (last 5 channels, horizontal scroll, SharedPrefs). `recordWatched()` on channel tap. Files: `live_channel_provider.dart`, `live_tv_screen.dart`. | ✅ DONE | 2026-07-22 | `b55d9f55` |
| LIVETV-CI-FIX | `live_tv_screen.dart` used `RaddColors` as a type annotation in 8 method signatures — `RaddColors` is a BuildContext extension, not a concrete class; `RaddTheme.of(context)` returns `RaddTheme`. Replace-all → `RaddTheme`. Dart analyze now passes. | ✅ DONE | 2026-07-22 | `36cf2740` |
| LOGO-AUDIT | Verified all 84 live-channel logo URLs. Tamashaweb URLs (44): all ✅ 200. Wikipedia URLs (31+): 11× HTTP 404 dead, 4× 400 (bad SVG-thumb format), 15× 429 rate-limited (unreliable); plus 6 wrong logos (e.g. `discovery.png` for CGTN Doc / Discover Pakistan / disc-science; `ary-musik.png` for 8xM; `madani-channel.png` for Saudi Makkah/Madinah; `tamasha.png` for Tamasha Woman/Life). Fixed all 41 — replaced with verified tamashaweb.com/wp-content/uploads/* URLs in `_live_seed` + added idempotent UPDATE migration in `init_db()` that patches existing Oracle DB rows on next restart. **Oracle deploy required** to apply to live data. | ✅ DONE | 2026-07-22 | `67a27b5c` |
| LIVETV-AUDIT-P0A — PAYWALL-ENFORCE | Non-free channels have no subscription gate — lock icon is visual only. Fix: check subscriptionProvider in _playChannel(); show paywall snackbar. | ✅ DONE — `e128942` | 2026-07-23 | — |
| LIVETV-AUDIT-P0B — SHARED-ANIM-CTRL | Single AnimationController for 84 cards = ~5000 AnimatedBuilder rebuilds/sec. Fix: replace card LIVE badges with static pills; keep pulse only on header dot and hero Watch Now. | ✅ DONE — `e128942` | 2026-07-23 | — |
| LIVETV-AUDIT-P0C — CHANNEL-ID-MATCH | Channel switcher matches current channel by name string, not ID. Fix: pass currentChannelId from widget.fileId; match ch.id. | ✅ DONE — `e128942` | 2026-07-23 | — |
| LIVETV-AUDIT-P1A — IMAGE-NETWORK | _LiveChannelSwitcherSheet uses Image.network — no cache, re-downloads all logos on every open. Fix: replace with CachedNetworkImage. | ✅ DONE — `e128942` | 2026-07-23 | — |
| LIVETV-AUDIT-P1B — GLOBAL-SEARCH | Search is category-scoped — searching 'BBC' on Sports tab misses BBC News. Fix: when query non-empty, search all channels ignoring chip selection. | ✅ DONE — `e128942` | 2026-07-23 | — |
| LIVETV-AUDIT-P1C — BACK-TO-ALL | No back affordance from single-category grid — hardware back exits the screen. Fix: add '← All Channels' row at top of grid. | ✅ DONE — `e128942` | 2026-07-23 | — |
| LIVETV-AUDIT-P1D — RECONNECT-OVERLAY | Live player shows generic spinner during stream stall. Fix: add 'Reconnecting…' label below spinner when _isLive && _buffering. | ✅ DONE — `e128942` | 2026-07-23 | — |
| LIVETV-AUDIT-P2A — HEXCOLOR-DEDUP | _hexColor() in live_tv_screen.dart and _parseHexColor() in _ps_ui_mixin.dart are identical. Fix: add shared hexColor() to live_channels.dart. | ✅ DONE — `e128942` | 2026-07-23 | — |
| LIVETV-AUDIT-P2B — FONT-SIZES | Recently-watched 9.5px, fallback 9px/8px — below legibility threshold. Fix: raise to 11px, 10px, 9px. | ✅ DONE — `e128942` | 2026-07-23 | — |
| LIVETV-AUDIT-P2C — CARD-WIDTH | _HorizontalCard 100px — logos cramped. Fix: widen to 120px; row height 155→165px. | ✅ DONE — `e128942` | 2026-07-23 | — |
| LIVETV-AUDIT-P2D — LIVE-METHODS-DEDUP | _buildLiveBottomArea() and _buildLivePortraitPanel() differ only by 8px padding. Fix: merge into _buildLiveArea({topPadding}). | ✅ DONE — `e128942` | 2026-07-23 | — |
| LIVETV-AUDIT-P2E — ADMIN-DOC-FIX | Admin panel says 'changes take effect immediately' but app TTL is 1 hour. Fix: correct the note. | ✅ DONE — `e128942` | 2026-07-23 | — |
| LOGO-AUDIT-2 | Re-audit all 84 live-channel logo_url values in db.py. tamashaweb migrated CDN from WordPress to canvas.tamashaweb.com — ALL old wp-content/*.png URLs return text/html (dead). Replaced with canvas.tamashaweb.com/*.webp URLs from tamashaweb HTML source (75/84 channels; 9 absent from live page kept). Replaced static _logo_patches dict with self-maintaining _live_seed loop covering all 84 channels. Oracle deploy required. | ✅ DONE | 2026-07-22 | `c372e136` |
| THEME-IMPORT-FIX | Add missing `radd_button`/`radd_chip`/`radd_radius` imports to 5 screens (live_tv, data_usage, quota_full, plan_expired, onboarding) — imports added by other-session agent, CI green. | ✅ DONE | 2026-07-23 | `f56e9540` |
| THEME-WIDGET-FIX | Actual widget replacements: GestureDetector+gradient-Container→`RaddButton.signal` (plan_expired, quota_full); SIMOSA-GestureDetector→`Material+InkWell`; chips GestureDetector+AnimatedContainer→`RaddChip` (live_tv); clear-search GestureDetector→`IconButton`; "Try again"→`RaddButton.tonal`; `BorderRadius.circular(AppRadius.md)`→`RaddRadius.mdRadius` ×6 (data_usage). CI `87455ea3` green. | ✅ DONE | 2026-07-23 | `87455ea3` |
| LIVE-P1 | **DVR metadata support.** Add `hasDvr`+`dvrWindowSeconds` to `LiveChannel` model (`fromJson`/`fromRow`/`toRow`/`hexColor` getter). DB migration v26→v27 (`ALTER TABLE live_channels ADD COLUMN has_dvr/dvr_window_seconds`). Oracle DDL + ALTER TABLE migrations + DVR patch (geo-news → has_dvr=1, dvr_window_seconds=3600). API `/api/live/channels` includes both fields. | ✅ DONE | 2026-07-24 | `458e650c` CI ✅ |
| LIVE-P2 | **Portrait player redesign (YouTube-style).** `_buildLivePortraitScaffold()` replaces full-bleed portrait layout for live: SafeArea header (back/title/settings) → 16:9 AspectRatio video box with `_buildLiveVideoBox()` (buffering/reconnecting/error/controls overlays) → identity bar (`_buildLiveIdentityBar()`: logo+name+LIVE dot) → inline scrollable channel list (`_buildLiveInlineChannelList()`). Gated by `if (_isLive)` early return in `_buildPortraitLayout()`. | ✅ DONE | 2026-07-24 | `a9497fb9` CI ✅ |
| LIVE-P3 | **Landscape improvements.** `_buildLiveLandscapeWatermark()` (channel logo, 20% opacity, bottom-right, always visible). `_switchToAdjacentLiveChannel(delta)` + `_onScaleEnd` live swipe gate (>500 px/s horizontal velocity → switch channel). | ✅ DONE | 2026-07-24 | `a9497fb9` CI ✅ |
| LIVE-P4 | **Live stream error/reconnecting UX.** `_buildLiveErrorOverlay()` inside video box: signal-off icon + error text + Retry + Switch Channel buttons. Reconnecting label below spinner in `_buildLiveVideoBox()`. | ✅ DONE | 2026-07-24 | `a9497fb9` CI ✅ |
| LIVE-P6 | **DVR URL audit — all 84 channels.** Wifi audit (tamashaweb accessible without Jazz SIM). Fetched `playlist_dvr_timeshift-0-3600.m3u8` variant for all 84 channel paths in parallel. **Result:** 3 confirmed DVR: geo-news (known), ary-news (new), ary-digital (new). All 81 others → 404/403. Updated `_live_seed` stream_urls for ary-news + ary-digital to DVR variant. Added ary-news + ary-digital to `_dvr_channels`. Oracle deployed `e26fbef8` → API ✅. | ✅ DONE | 2026-07-24 | `3b373164` Oracle ✅ |
| LIVE-P7-A | **Quality selector — built.** Live audit first: fetched all 84 master playlists live. 32 `YlUHeDQb7a`-path channels → 403 (likely Jazz-SIM-gated or token-auth); ~29 `jazzauth` masters → 2–3 real renditions; ~7 jazzauth → 1 rendition only; ~6 numeric-path → already at rendition level; ~9 → 404 dead. Built runtime picker: `_fetchLiveRenditions()` fetches master on every channel open, `_parseLiveRenditions()` extracts `#EXT-X-STREAM-INF` blocks and resolves relative URLs. Settings panel: shows real chevron picker when ≥2 renditions available, informational "Auto (ABR)" otherwise. `_switchLiveRendition(i)` re-fetches master (fresh `nimblesessionid`), picks Nth rendition, calls `_player.open()`. Graceful degradation for 403/404/single-rendition channels. | ✅ DONE | 2026-07-24 | `072a0b89` CI ✅ |
| NET-STREAM-1 | **Direct network stream playback.** `contentType='network'` branch in `_openMedia()` — direct `_player.open()`, no JazzDrive/quota. Share-sheet: `ACTION_SEND text/plain` intent-filter + `extractSharedText()` in MainActivity. Cold/warm-start intent handlers updated to route http/https URLs as `network` not `movie`. "Play from URL" row at top of Local Media Videos tab (paste dialog, scheme auto-prepend, HLS rendition picker reused). | ✅ DONE — `f08dcad1` CI ✅ | 2026-07-25 | — |
| LIVE-P7-B | **Slim live settings panel.** `_openLiveSettingsPanel()`: Quality row (Auto ABR, informational), Audio Track (if multi-track → opens `_openAudioPanel()`), Sleep Timer (picker at 15/30/60/90 min). Portrait header settings icon + portrait video-box controls overlay settings button both wired to new panel. VOD `_openSettingsPanel()` unchanged. | ✅ DONE | 2026-07-24 | `574db7d8` CI ✅ |
| UI-POLISH-2026-07-25 | **Home screen UI review polish pass.** 7 targeted improvements from third-party review: (1) avatar 46/40px → 40/34px (~15% smaller, header breathing room); (2) greeting font 13→15px (more prominent); (3) category chip right-margin 8→10px, inactive border `t.textMuted.withOpacity(0.35)` width 1.2 (better contrast); (4) SimosaCard Row `crossAxisAlignment` center→start, CTA Column `crossAxisAlignment.end` (Claim button aligns with title); (5) `_ContentSection` subtitle field added — "Fresh this week", "No subscription needed", "Still airing", "What everyone's watching", "Just added" per section; (6) nav icon 22→24px (+10%); (7) hero gradient stronger (stops 0.35→0.25, 0.7→0.6; bottom color `0xDD000000`→`0xFF0A0A1E` midnight-indigo depth). | ✅ DONE — `3fe2021` CI ✅ | 2026-07-25 | — |
| PROFILE-POLISH-2026-07-25 | **Profile screen audit — 9 issues from third-party review.** (1) `_Section` StatelessWidget → ConsumerWidget with glass-card body (ClipRRect + RaddElevation.blurWrap, canBlur-gated) + specular top-border; (2) dash header → 5 px colour-coded dot per section (General/Appearance/MyContent/Account=primary, Player=warning, Device=info, MyStats=primary); (3) 12 key `_SectionTile` calls gain `subtitle:` strings; (4) `_StatsCard` header/Container stripped — call site now wrapped in `_Section(title: 'My Stats')`; (5) stat value font 13→14px, label font 10→11px (both AnimatedBuilder + static Text); (6) greeting font 13→15px; (7) divider `indent: 52`→54; (8) Manage TextButton `color: AppColors.primary`; (9) all 5 section gap `SizedBox(height: 12)` → `RaddSpace.md` (16 px). | ✅ DONE — `ab235f4d` CI ✅ | 2026-07-25 | — |
| PROFILE-AUDIT-1 | **Bug: Missing `mounted` guard in `_loadExtras()`** — `setState()` at L106 fires after `getQuota()` async gap with no `mounted` check → crashes if user navigates away mid-flight. Fix: add `if (!mounted) return;` before L106 `setState`. | ✅ DONE | 2026-07-25 | profile_screen.dart |
| PROFILE-AUDIT-2 | **Bug: `_StatsCard` FutureBuilder recreates DB query on every parent rebuild** — `LocalDb.getWatchStats()` called directly as `future:` in a StatelessWidget; every connectivity/tap rebuild fires a new DB query and flickers stats back to spinner. Fix: convert to StatefulWidget, cache future in `initState()`. | ✅ DONE | 2026-07-25 | profile_screen.dart |
| PROFILE-AUDIT-3 | **UX: "Upgrade Plan" tile shown to active subscribers** — Account section always shows "Upgrade Plan" + "Get more content and storage" even when user has an active sub. Should show "Manage Plan" + "Adjust or renew your plan" when `user?.hasActiveSubscription == true`. | ✅ DONE | 2026-07-25 | profile_screen.dart |
| PROFILE-AUDIT-4 | **UX: Close (×) button shown when screen is embedded as a tab** — Header always renders `IconButton(pop)` regardless of `showBottomNav`. When embedded in IndexedStack (`showBottomNav=false`) there is nothing to pop — tap is a no-op or exits unexpectedly. Wrap in `if (widget.showBottomNav)`. | ✅ DONE | 2026-07-25 | profile_screen.dart |
| PROFILE-AUDIT-5 | **UX: "My Stats" section appears before "My Content"** — Stats summarise content activity so should follow Watchlist/History, not precede them. Swap section order. | ✅ DONE | 2026-07-25 | profile_screen.dart |
| PROFILE-AUDIT-6 | **UX: "General" section is a full glass card for a single tile** — One-item section with header + card is visually heavy. Merge Settings tile into Appearance section and rename to "General". | ✅ DONE | 2026-07-25 | profile_screen.dart |
| PROFILE-AUDIT-7 | **Code: Redundant `setState(() {})` after EditProfileScreen return** — L228, L324 call bare `setState(() {})` on EditProfileScreen pop. `user` is already from `ref.watch(authProvider)` — Riverpod rebuilds automatically. Remove the setState calls. | ✅ DONE | 2026-07-25 | profile_screen.dart |
| PROFILE-AUDIT-8 | **Code: Non-const SizedBox at L541 and L659** — `SizedBox(height: RaddSpace.md)` (L541) and `SizedBox(height: RaddSpace.lg)` (L659) missing `const`. Both RaddSpace values are static constants. | ✅ DONE | 2026-07-25 | profile_screen.dart |
| HANDOFF-CLEANUP-2026-07-25 | **Docs: fix stale HANDOFF + profile_screen polish.** (1) HANDOFF top section updated: CI ⏳→✅; stale "Archived" section (described 9 profile items as "not yet implemented" when all were done in `ab235f4d`/`73af7f91`) deleted — would have sent next agent on duplicate work. (2) profile_screen.dart: "Debug Logs" `_SectionTile` was the only tile in the Account section with no `subtitle` — added `'Internal diagnostic logs'` for consistency with all other tiles. | ✅ DONE | 2026-07-25 | profile_screen.dart + AGENT_HANDOFF.md |

| AUDIT-2026-07-25 | **Code audit — parallel audit of NET-STREAM-1, subscription, login/register, player audiolab, broad codebase.** 5 confirmed bugs found and logged as individual OPEN rows below. No code changes made this session. | ✅ DONE (audit only) | 2026-07-25 | — |
| BUG-REGISTER-GUEST | **Bug: `_guest()` catch block in `register_screen.dart` calls `setState()` without `mounted` guard.** Fixed: added `if (!mounted) return;` before all three unguarded `setState()` calls in `_register()` (DioException + generic catch) and `_guest()` catch. | ✅ DONE | 2026-07-25 | `register_screen.dart` |
| BUG-TID-MOUNTED | **Bug: `_poll()` in `tid_status_screen.dart` calls `setState()` after an async gap without mounted guards.** Fixed: added `if (!mounted) return;` before the approved and rejected `setState()` calls inside `_poll()`. | ✅ DONE | 2026-07-25 | `tid_status_screen.dart` |
| BUG-AUDIOLAB-RAW-ERR | **Bug (Phase L): `_ps_audiolab_mixin.dart` exposes raw exception via snackbar.** Fixed: replaced `'Dub error: $e'` with `'Audio dubbing error — please try again'`. | ✅ DONE | 2026-07-25 | `screens/player/_ps_audiolab_mixin.dart` |
| BUG-NET-URL-TITLE | **UX: URL title extraction includes query params.** Fixed: strip query string with `.split('?').first` before `Uri.parse` in `main.dart`, `splash_screen.dart`, and `local_media_screen.dart`. | ✅ DONE | 2026-07-25 | `main.dart`, `splash_screen.dart`, `local_media_screen.dart` |
| BUG-NET-NO-VALIDATION | **UX: No URL validity check in `local_media_screen.dart` before opening player.** Fixed: `Uri.tryParse` validation with scheme/host check; shows SnackBar error for invalid input. | ✅ DONE | 2026-07-25 | `local_media_screen.dart` |
| BUG-SUB-STYLE-FIXES | **Two subtitle regression fixes.** (1) `onSubPropertyChanged` guard in `_ps_subtitle_mixin.dart` set `sub-ass-override='force'` for only 9 props; `sub-align-x`, `sub-align-y`, `sub-margin-x`, `sub-ass-scale-with-window` were missing → position tab changes silently ignored on ASS/embedded subs. Fix: drop the per-prop whitelist and always set `force` for any non-internal prop. (2) `_saveSubPrefs()` in `_ps_panels_subtitle.dart` never wrote `pref_sub_margin`, so the bottom-margin slider reset to 100 px every time the panel reopened. Fix: add `await prefs.setDouble('pref_sub_margin', _subBottomMargin)` to `_saveSubPrefs()`. | ✅ DONE | 2026-07-26 | `9b3b9a8b` CI ✅ |
| BUG-SUB-STYLE-REAPPLY | Reapply the complete saved native MPV subtitle style after subtitle tracks arrive, after a preferred/manual subtitle track is selected, and after companion subtitle loading. MPV can recreate its subtitle renderer after these lifecycle events and otherwise restore the default small white-on-black style. | ✅ DONE | 2026-07-29 | confirmed in code — all 3 reapply call sites verified at HEAD (`stream.tracks.listen` microtask, `_applyCompanionSub`, `onSubtitleTrackSelected`) |
| AUDIO-LAB-INVESTIGATION | **Investigation: audio lab filters reported as having no audible effect.** Full static code trace: `_applyLabAf()` → `widget.onLabAfChanged()` → `_currentLabAf = afStr; _applyAllAf()` → `_buildMergedAfString()` → `_np.setProperty('af', ...)`. Chain is complete and correct. Lab state (`_labVocal`, `_currentLabAf`, etc.) IS restored from SharedPrefs at startup (`player_screen.dart` lines 481–545), with `_applyAllAf()` called after 500ms. All previously known bugs (A1–A6, BUG-AUDIO-SILENT-01, Lab EQ coupling) are confirmed fixed in current HEAD. Prior session audit (AUDIO-PANEL-SAVE) also confirmed all Lab callbacks correct. Conclusion: no actionable code bug found; the earlier concern was based on incomplete grep of `_ps_playback_mixin.dart` (prefs live in `player_screen.dart`). | ✅ DONE (investigation) | 2026-07-26 | — |
| DOC-CLEANUP-2026-07-26-P2 | **Docs cleanup pass 2: fix wrong info across 6 files, delete 2 stale files.** Deleted: `.agents/memory/live-p6-p7a-jazz-sim.md` (wrong — claimed CDN requires Jazz SIM, P6/P7-A blocked; both confirmed done, CDN globally accessible) and `jazzdrive_research/HANDOFF.md` (stale one-off handoff from 2026-06-15, wrong workflow rules). Fixed: `PRODUCT_CONTEXT.md` (dead `MASTER_TASKLIST.md` ref → `TASKS.md`), `TEN_POINT_PLAN.md` (date + 139/141 completion note, G4+K5 blocked), `PLAYER_SPEC.md` (stale "4521/7087 lines" header + note about Phase J mixin split), `PLAYER_GUIDE.md` (stale "7033 lines" header + mixin-split note). | ✅ DONE | 2026-07-26 | `ca365a5f` |
| DOC-CLEANUP-2026-07-26 | **Docs cleanup: trim HANDOFF, fix stale CI status, delete 7 obsolete files.** `AGENT_HANDOFF.md` trimmed from 2022 → 37 lines (stacked historical sections removed; all that detail already in TASK_LOG.md). CI status on `9b3b9a8b` updated ⏳→✅ in both HANDOFF and TASKS.md. Deleted: `AGENT_PROMPT_AUDIO_LAB.md`, `AUDIO_LAB_BUGFIX_PLAN.md`, `PLAYER_UX_BB_PLAN.md` (BB done), `LIBRARY_REDESIGN_PLAN.md` (marked SUPERSEDED), `reports/2026-07-04-audit-and-plan.md`, `tasks/DA-2-watch-integrity-smc.md` (DA-2 done), `LIVE_PLAYER_PLAN.md` (at repo root, all phases done). Fixed stale `agent-hub/archive/` reference in `DOWNLOAD_TAB_REDESIGN_PLAN.md` (no archive/ dir per rules). | ✅ DONE | 2026-07-26 | `66e6b1f4` |
| AUDIT-FLUTTER-2026-07-26 | **Comprehensive Flutter app audit (226 files, ~81K lines).** 8 parallel subagents covered: security layer, API/networking, player logic, player widgets, all screens, providers, services/DB, shared widgets. Findings: 5 CRITICAL, 12 HIGH, 27 MEDIUM, 9 LOW. Full details in TASK_LOG.md session 2026-07-26. | ✅ DONE (audit) | 2026-07-26 | — |
| SEC-01 | **CRITICAL: Production API served over plain HTTP (http://92.4.95.252).** Requires a registered domain + TLS cert — cannot be actioned without an external domain purchase. Tracked as a known limitation in `agent-hub/memory/infrastructure-constraints.md`. | ⛔ BLOCKED — needs domain | 2026-07-26 | constants.dart, remote_config.dart |
| SEC-02 | **CRITICAL: Debug Diagnostics screen accessible to all users** via secret 5-tap gesture on version text. Exposes live logs, E2E JazzDrive playback timelines, and server communication paths. Fix: gate behind `kDebugMode` or restrict to accounts with an `is_admin` flag. | ✅ DONE | 2026-07-26 | Already fixed: profile_screen.dart L647 gates entry behind `kDebugMode \|\| user?.isAdmin == true` |
| SEC-03 | **CRITICAL: Auth server error falls through to guest navigation.** Login/register fails to throw on server error → user navigates to Home as guest instead of seeing retry prompt. Breaks intended Members Only access flow. | ✅ DONE | 2026-07-26 | Already fixed: login_screen.dart BUG-LOGIN-01 + register_screen.dart BUG-REGISTER-01 both check `s.error != null` and return early |
| SEC-04 | **HIGH: Vault PIN uses static salt (SHA-256).** Fix requires per-user random salt + PBKDF2, but migration would break all existing vault PINs — users would need to re-enter vault PINs once. Tracked in `infrastructure-constraints.md`. | ⛔ BLOCKED — needs migration decision | 2026-07-26 | vault_service.dart:87 |
| SEC-05 | **HIGH: APK signature check placeholder.** `RADDFLIX_CERT_SHA256_PLACEHOLDER` in `app_guard.dart`. Cannot fix without the release signing keystore SHA-256 — must be extracted from the keystore file with `keytool`. Tracked in `infrastructure-constraints.md`. | ⛔ BLOCKED — needs keystore | 2026-07-26 | app_guard.dart:47 |
| BUG-FREE-EP-02 | **CRITICAL: `_isFree` revenue bug — re-verify in current HEAD.** `_isFree` flag may remain `true` on content transition in player_screen.dart (~L1099-1105). Confirmed in prior audit as unfixed. Re-verify in current mixin-split layout and fix if still present. | ✅ DONE | 2026-07-26 | Already fixed: `_ps_playback_mixin.dart` L913-925 has BUG-C02 fix — updates `_isFree`/`_trackUsage` per episode on transition |
| BUG-DOWNLOAD-SIZE | **HIGH: Download service deletes nearly-complete files** on 2% size mismatch (`fileSize < expectedTotal * 0.99`). On unstable 3G networks this kills valid large downloads. Fix: implement Range (resume) support instead of delete-and-retry. | ✅ DONE | 2026-07-26 | Already fixed: download_service.dart L173-181 uses `tooSmallAbsolute \|\| tooSmallVsExpected` guard; BUG-DL-VALIDATE-01 comment confirms |
| BUG-CATALOG-LISTENER | **HIGH: CatalogNotifier leaks Connectivity stream subscription** on re-initialization — multiple concurrent syncs, growing memory usage. Fix: cancel existing subscription before re-assigning in `initialize()`. | ✅ DONE | 2026-07-26 | Already fixed: catalog_provider.dart L121 does `_connectivitySub?.cancel()` before reassign; dispose() also cancels at L145 |
| BUG-EPISODE-SORT | **HIGH: Episode sort order in show_detail_screen affects player's "next episode" argument.** Toggling UI sort reverses in-player next/prev navigation — user ends up watching wrong episode order. Fix: always pass absolute ascending sequence to player regardless of UI sort state. | ✅ DONE | 2026-07-26 | Already fixed: show_detail_screen.dart M02 fix — UI sort (`_sortAscending`) only affects display list; player always receives `allSeasonsEps` sorted independently |
| BUG-BINGE-TIMER | **HIGH: BingeGuardController timer leaks** if widget is removed from tree without calling `onPause`. Timer keeps firing `onThreshold` callbacks on a dead context. Fix: verify `PlayerScreen.dispose()` always calls `_bingeGuard.dispose()`. | ✅ DONE | 2026-07-26 | Already fixed: binge_guard_controller.dart has `_isDisposed` guard in `onPlay()` (L-05 FIX comment); BingeGuardController not directly instantiated in player_screen.dart |
| BUG-TIMELINE-SYNC | **HIGH: PlaybackTimeline uses synchronous file I/O (`writeAsStringSync`) on the main thread** during player startup. Causes UI jank on budget MediaTek devices (the primary target). Fix: use IOSink or async writes. | ✅ DONE | 2026-07-26 | `4697dc2e` — `_append()` now uses `File.writeAsString(...).ignore()` (async fire-and-forget) |
| BUG-XOR-CLOCK | **MEDIUM: XOR session key clock tolerance.** Already fixed server-side: `request_encoding.py` `_candidate_keys()` tries offsets 0, −1, +1 so ±1h clock skew is tolerated. `XorWsgiMiddleware` is mounted in `app.py`. No client-side change needed. | ✅ DONE | 2026-07-26 | Already in request_encoding.py (verified 2026-07-26) |
| BUG-LOCAL-MEDIA-IO | **MEDIUM: `queryAllVideos` parallel subtitle I/O storm on large libraries.** Fixed: subtitle checks now run in batches of 20, bounding concurrency without removing per-video subtitle detection. | ✅ DONE | 2026-07-26 | `3b23881` — services/local_media_service.dart |
| BUG-DB-DELETE-RISK | **MEDIUM: `_openDb` deletes the entire local DB on any key error**, not just "not a database" errors. A transient Keystore hardware failure would wipe all history, downloads, and user data. Fix: check specifically for SQLite "not a database" error code before deleting. | ✅ DONE | 2026-07-26 | `a096c99b` — catch changed from `catch (_)` to `catch (e)` with `errMsg.contains('not a database')` guard; non-matching errors rethrow |
| BUG-PROFILE-PIN | **MEDIUM: Profile PINs stored as plaintext in SQLite.** On rooted devices, anyone with DB access can bypass profile locks. Fix: store bcrypt/SHA-256 hash instead of raw PIN. | ✅ DONE | 2026-07-26 | `478a5ecb` — `LocalDb.hashProfilePin()` (SHA-256, `raddflix_profile_pin_` salt); `createProfile`/`updateProfile` hash before insert; `selectProfile` uses hash compare + transparent legacy migration |
| BUG-PLAYER-AUTODISPOSE | **MEDIUM: `playback_service.dart` disposes player on `stream.completed`**, which kills the session before any auto-play-next queue can start. Fix: only dispose if the queue is truly empty. | ✅ DONE | 2026-07-26 | `51db4546` — `hasNext` guard: only calls `stop()` when `episodeIndex >= episodes.length - 1` |
| BUG-VOICE-STUB | **MEDIUM: Voice Commands feature fully wired in UI settings but non-functional** — `requestPermission()` always returns `false`, `start()` is a no-op. Users see the setting and assume it's broken. Fix: hide the setting behind a feature flag or show "Coming Soon" label. | ✅ DONE | 2026-07-26 | `81182f0f` — replaced `_QsToggleRow` with inline disabled row; label dimmed, "Coming soon" chip, `Switch.onChanged: null` |

---

## Home Screen Visual Audit — Bug Fix Batch (2026-07-28)

> Full audit report in `agent-hub/history/TASK_LOG.md`. All 11 issues identified from APK screenshots.
> Fix order: critical → medium → minor. All fixes are Flutter-only (no Oracle/backend changes needed).
> Rule 42 applies: `log_pending.sh` → edit → `auto_commit.sh` for every task.

| Task | Description | Severity | Status | Commit |
|---|---|---|---|---|
| HS-01-SIMOSA-DISMISS | **SIMOSA card dismiss not persisted — reappears on every cold start.** `_dismissed` is an in-memory `bool` in `_SimosaCardState`, reset to `false` on every app launch. `_onDismiss()` only calls `setState(() => _dismissed = true)` — zero `LocalDb` or `SharedPreferences` write. Fix: call `LocalDb.setSimosaDismissed(true)` on dismiss; check `LocalDb.getSimosaDismissed()` in `initState()` alongside `_loadStreak()`; reset after 24 h or on next claim. | 🔴 Critical | ✅ DONE | `b6934fb` — `_onDismiss()` now async; writes `simosa_dismissed_until` (Unix ms + 24h) to SharedPrefs; `_loadDismissed()` called in `initState` to restore state; claim clears the key |
| HS-02-SIMOSA-CARD-SIZE | **SIMOSA card too tall (~83px) and body text wraps to two lines.** Right-column layout (Claim button ~34px + gap 2px + dismiss icon 23px = 59px) + inner padding 16px + outer padding 8px = ~83px total. `'Claim your free Jazz data via SIMOSA'` at `fontSize: 11.5` inside an `Expanded` column has no `maxLines` — "SIMOSA" wraps to line 2 on 360dp screens. Fix: constrain card height (single-line layout, smaller dismiss icon, reduced padding); add `maxLines: 1, overflow: TextOverflow.ellipsis` to body text. | 🔴 Critical | ✅ DONE | `b6934fb`+`b5e83bc` — `maxLines:1`+`overflow:ellipsis` (b6934fb); inner padding 8→6px, Claim button 7→5px, dismiss icon 15→12px/padding 4→3px (b5e83bc) |
| HS-03-SIMOSA-NAV-CLIP | **SIMOSA card bottom edge cut off by bottom nav bar.** With `extendBody: true`, the 72px bottom clearance (`home_screen.dart` L570) is insufficient — the bottom nav totals ~92px (62px bar + 10px padding + ~20px system gesture bar). The SIMOSA card's dismiss X button area is partially obscured by the floating nav. Fix covered by HS-05 (raise clearance to 96px). | 🔴 Critical | ✅ DONE | covered by HS-06 (`10aaa85`) — bottom clearance raised to 96px |
| HS-04-APPBAR-SPACER | **Greeting "Good evening" bleeds into RaddFlix AppBar logo.** `SizedBox(height: 48)` spacer (`home_screen.dart` L368) is too small. With `extendBodyBehindAppBar: true`, body starts at y=0; status bar (~28px) + toolbar (56px) = 84px total AppBar height. Greeting at y=50 overlaps the transparent AppBar and the final "g" of "evening" appears beside the logo. Fix: replace `48` with `MediaQuery.of(context).padding.top + kToolbarHeight`. | 🔴 Critical | ✅ DONE | `10aaa85` |
| HS-05-FILTER-DUPE-CARDS | **Duplicate content cards when a non-All filter chip is selected.** "Trending Now" SliverGrid (`home_screen.dart` L472–565) renders unconditionally regardless of active filter. When "Shows" is selected, the same posters appear in both the Trending shelf and the filtered SliverGrid within the same viewport. Fix: gate the Trending shelf inside `if (_activeFilter == 'All')` so it only renders when no filter is active. | 🔴 Critical | ✅ DONE | `10aaa85` |
| HS-06-NAV-BLEED | **Content text bleeds under the translucent bottom nav bar.** Bottom nav total height ~92px (62px bar + 10px bottom padding + ~20px system home gesture bar). Current clearance `SizedBox(height: 72)` is ~20px short — card titles and "FREE" badge text are visible beneath the nav on all screenshots. Fix: raise to `SizedBox(height: 96)` (safe on all device heights). | 🔴 Critical | ✅ DONE | `10aaa85` |
| HS-07-SYNC-BANNER-POS | **"Syncing catalog…" pill banner pushes the hero down instead of floating over it.** Banner is a `SliverToBoxAdapter` inserted between the greeting and the hero (`home_screen.dart` L400–422). When visible it shifts the hero further down the viewport — breaking the full-bleed top-of-feed hero layout. Fix: move the banner inside the hero's `Stack` as a `Positioned` overlay (top-aligned, center-horizontal), or into the AppBar `flexibleSpace`. | 🟠 Medium | ✅ DONE | `10aaa85` |
| HS-08-HERO-TRANSITION | **Hero PageView shows all three adjacent slides simultaneously during auto-scroll.** The `rotateY` + perspective `Transform` on `_HeroCard` (`home_screen.dart` L884–895) pushes card edges outside each page slot's clip bounds. During `animateToPage`, left and right neighbours bleed through the rotating current card — producing a 3-poster side-by-side look. Fix: wrap the `Transform` in a `ClipRect`; or switch hero transition to a cross-fade (`AnimatedSwitcher`) instead of 3D rotate. | 🟠 Medium | ✅ DONE | `10aaa85` |
| HS-09-HERO-ORPHAN | **Greeting appears as floating orphan text with large black void during catalog load.** When `catalog` is non-empty but the hero's `CachedNetworkImage` hasn't resolved yet, the near-black placeholder blends into the background — giving the visual sequence: AppBar → ~200px black gap → "Good evening" → hero image resolves. Fix: add an explicit `SizedBox`-height placeholder (or shimmer) inside the hero slot so the layout is stable before the image loads. | 🟠 Medium | ✅ DONE | `10aaa85` |
| HS-10-FLOAT-CTRL-BUILD | **`_floatCtrl.repeat()` called inside `build()` — causes mid-cycle animation jank.** Lines 819–822: `if (!_floatCtrl.isAnimating) _floatCtrl.repeat(...)` inside `build()`. Any parent widget rebuild (catalog update, provider notify) can interrupt the sine cycle mid-frame and restart the 3D tilt from zero — visible as a snap in the hero float animation. Fix: move `_floatCtrl.repeat(reverse: true)` to `initState()` and remove the guard from `build()`. | 🟡 Minor | ✅ DONE | `10aaa85` |
| HS-11-CHIP-SCROLL-HINT | **"English" filter chip invisible — no scroll hint on category chip row.** The chip row renders: All / Movies / Shows / Dramas / Urdu / Punjabi — "English" is cut off with no trailing fade, shadow, or scroll affordance. Users have no visual signal that the row is scrollable. Fix: wrap the `ListView.builder` in a `ShaderMask` with a right-edge fade gradient to reveal that more options exist. | 🟡 Minor | ✅ DONE | `10aaa85` |

| SUB-OVERLAY-FIX | **Architecture fix: subtitle customization never worked — Flutter SubtitleOverlay was never wired up.** Root cause: (1) `Video(...)` lacked `SubtitleViewConfiguration(visible:false)` so MPV rendered inside the SurfaceView, unaware of Flutter controls; (2) `SubtitleOverlay` widget existed but was never placed in any Stack; (3) `player.stream.subtitle` stream was never listened to. Fix: added `SubtitleViewConfiguration(visible:false)` to Video widget, subscribed to `player.stream.subtitle` → `_currentSubLine`, placed `SubtitleOverlay` in both landscape and portrait stacks. All subtitle style customization now works instantly and reliably. See Rule 51 in RULES.md. | ✅ DONE | 2026-07-29 | `defb61e` — 4 files: `_ps_ui_mixin.dart`, `_ps_subtitle_mixin.dart`, `_ps_playback_mixin.dart`, `player_screen.dart` |

| AUDIO-FIX-1 | **lavfi-complex + audio-file not reset on episode nav / new video open.** AI Dub sets `lavfi-complex` and `audio-file` MPV properties that survive `_player.open()` calls. Any user who opened the AI Dub panel carried those properties into every subsequent video → complete silence. Fix: reset both in `_playEpisodeAt()` and `_openMedia()` (mirrors the existing `_disableDubMode()` resets). | ✅ DONE | 2026-07-29 | `c84e7f13` — `_ps_playback_mixin.dart` |
| AUDIO-FIX-2 | **`_applyAllAf()` fired on blind 500ms timer — race condition caused silent AF discard.** If MPV's audio pipeline hadn't opened yet when the timer fired, `setProperty('af', ...)` was accepted at IPC level but the filter graph was silently discarded. Fix: move `_applyAllAf()` call into `_player.stream.tracks.listen` handler — fires only after MPV confirms audio tracks exist, guaranteeing the decoder is ready. Also re-applies after EAC3/DTS hwdec SW-fallback. | ✅ DONE | 2026-07-29 | `6d8b7f4f` — `_ps_playback_mixin.dart`, `_ps_audiolab_mixin.dart` |
| AUDIO-FIX-3 | **`aformat=channel_layouts=stereo` only prepended when `pan=` filter present.** For users with no Balance/Vocal/Channel-Mode active, 5.1/mono tracks entered channel-count-sensitive filters with no normalisation → silent failure. Fix: always prepend `aformat` when the filter chain is non-empty (safe no-op for stereo content). | ✅ DONE | 2026-07-29 | `37aa08f0` — `_ps_audiolab_mixin.dart` |
| BUG-SUBLINE-ABSTRACT | **`_currentSubLine` undefined in `_PlayerPlaybackMixin` and `_PlayerUIMixin`** after SUB-OVERLAY-FIX added writes/reads in both. Fix: abstract getter/setter pair added to both mixins' cross-cluster declarations. | ✅ DONE | 2026-07-29 | `77c63061` — `_ps_playback_mixin.dart`, `_ps_ui_mixin.dart` |
| BUG-APPLYALLAF | **`_applyAllAf()` missing from `_PlayerPlaybackMixin` cross-cluster declarations.** AUDIO-FIX-2 added calls at lines 477+515 but the abstract stub was never added → dart analyze `undefined_method` on every build since. Fix: one-line abstract declaration added to cross-cluster methods block. | ✅ DONE | 2026-07-29 | `fd21ebb6` — `_ps_playback_mixin.dart` |

---

## Input Style Consistency (2026-07-29)

> Audit of every raw `TextField` in the app; match all to the login page `RaddTextField` style (52dp, `t.surface` fill, `t.border` 1px enabled → `AppColors.primary` 1.5px focused, `RaddRadius.mdRadius`).
> **Player overlay sheets are exempt** (white-on-dark, intentional): `jump_to_panel`, `jump_to_sheet`, `sleep_timer_sheet`, `color_picker_sheet`, `subtitle_hunter_sheet`.
> **Search screen (`search_screen.dart`) is exempt** — deliberate premium glass-pill with `BackdropFilter`.

| Task | Description | Status | Date | Commit |
|---|---|---|---|---|
| BGAUDIO-SESSION | **Configure `audio_session` at player init so Android holds audio focus in background.** Added `_configureAudioSession()` to `_ps_playback_mixin.dart`; called fire-and-forget from `_initPlayer()` on both fresh-create and reattach paths. Import added to `player_screen.dart`. | ✅ DONE | 2026-07-30 | `321b78e` |
| BGAUDIO-VID | **Drop video track (`vid=no`) when backgrounding with BG audio on; restore (`vid=auto`) on resume.** Added in `didChangeAppLifecycleState` in `player_screen.dart`. `_np.setProperty('vid','no')` on paused+bg; `_np.setProperty('vid','auto')` on resumed when `_isInBackground` was true. | ✅ DONE | 2026-07-30 | `321b78e` |
| BGAUDIO-UI | **Remove duplicate BG audio button from top bar — keep sidebar only.** Deleted the headphones icon that lived in `_buildTopBar()` next to the battery/clock HUD. The sidebar `bgaudio` item and the lock-screen quick-toggle remain. | ✅ DONE | 2026-07-30 | `69fbe76e` |
| INPUT-STYLE | **Standardise all non-exempt TextFields to login page style.** Five files patched: `live_tv_screen.dart` (search bar: `Container` → `AnimatedContainer` 52dp, `FocusNode` + animated focus border), `vault_screen.dart` (two dialog TextFields: `t.bg` → `t.surface`, `BorderRadius.circular(10)` → `RaddRadius.mdRadius`, add `enabledBorder`/`focusedBorder`), `vault_settings_screen.dart` (`_pinField`: same border + fill fix, keep `letterSpacing:8`), `local_folder_screen.dart` (`_buildSearchBar`: `BorderSide.none` → proper `t.border`/`AppColors.primary` pair), `local_media_screen.dart` (`_buildSearchBar` + URL dialog TextField: `smRadius` → `mdRadius`, consistent borders). | ✅ DONE | 2026-07-29 | `e3b828ea` |
