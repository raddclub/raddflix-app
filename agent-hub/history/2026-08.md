# RaddFlix Task Log — August 2026

## Session 2 — Live TV cluster (I-01, I-02, I-03, O-01, V-01 verified)

### Summary
Implemented the full live-TV bug cluster from LIVE_TV_FIXES.md backlog.

### Changes

**V-01 (verified existing):** Player already bypasses JazzDrive for `contentType == 'live'` at
`_ps_playback_mixin.dart:633`. Confirmed in-place; no code change needed.

**I-01 — DB schema v27→v28 (logo_path column):**
- `lib/core/constants.dart`: `catalogDbVersion` bumped to 28.
- `lib/core/db/local_db.dart`: `CREATE TABLE live_channels` gains `logo_path TEXT NOT NULL DEFAULT ''`.
- `lib/core/db/local_db.dart`: `_migrate()` `oldV < 28` block ALTERs existing tables.
- `lib/core/db/local_db.dart`: Added `saveChannelLogoPath(channelId, path)` and
  `getChannelLogoPath(channelId)` helper methods.

**LiveChannel model:**
- Added `logoPath` nullable field; `fromRow` reads and maps `logo_path`; `toRow` intentionally
  omits it (owned exclusively by PosterService/saveChannelLogoPath, not overwritten on sync).

**I-02 — PosterService channel-logo cache:**
- `lib/core/services/poster_service.dart`: Added `getChannelLogoPath(channelId)` (disk check)
  and `downloadAndCacheChannelLogo(channelId, url)` (download + persist to SQLite).
- Files stored as `ch_{sanitizedId}.jpg` in the same hidden `.raddflix_media` folder.

**I-03 — disk-first _ChannelLogo widget:**
- Added `_ChannelLogo` StatefulWidget in `live_tv_screen.dart`.
  Priority: disk (`logoPath` field, then PosterService check) → CachedNetworkImage + background
  save → fallback icon.
- Replaced all four `CachedNetworkImage` logo usages (hero banner, horizontal card, grid card,
  recently-watched row) with `_ChannelLogo`.
- Added `dart:io` and `poster_service.dart` imports.

**O-01 — Orientation fix:**
- `lib/screens/player_screen.dart` `initState`: `setPreferredOrientations` and
  `_setNativeOrientation('sensor')` are now guarded by `widget.contentType != 'live'`.
  Live TV leaves the device in whatever orientation the user was already holding.

### Flutter/Dart runtime
Dart SDK was unavailable in this workspace session; changes were verified by static inspection
and grep. Runtime validation requires an Android device/emulator with a debug APK build.

## Session 1 — VIBE-5D direct voice phrase mappings

- Made the five required direct phrases explicit in the live command parser:
  `slowed`, `nightcore`, `lofi`, `reverb`, and `no vibe`.
- Preserved optional aliases such as `slowed reverb`, `night core`, `lo-fi`, `normal`,
  `original`, and `vibe off`.
- Added parser regression tests covering all five required mappings plus case and whitespace
  normalization.
- `git diff --check` and repository preflight passed. Flutter/Dart executables were unavailable
  in the workspace, so the Dart test suite could not be executed locally.